<nav class="sidebar">
    @foreach (var item in Items)
    {
        <button type="button"
                class="sidebar-item @(item.Key == SelectedKey ? "active" : null)"
                @onclick="() => SelectAsync(item.Key)">
            <img src="@item.IconPath" alt="" />
            <span>@item.Label</span>
        </button>
    }
    <div class="sidebar-build">Build @BackupService.BuildInfo.Number</div>
</nav>
