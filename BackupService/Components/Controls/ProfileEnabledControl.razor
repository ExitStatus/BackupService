<div class="profile-enabled">
    <ToggleButton Value="Value" ValueChanged="OnToggle" Disabled="Disabled" />
    <span class="swatch @(Value ? "on" : "off")" title="@(Value ? "Enabled" : "Disabled")"></span>
</div>
