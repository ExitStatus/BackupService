{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "GoogleDrive": {
    "_comment": "The app's built-in Google OAuth client for one-click Drive authorization. Leave blank here and set real values via 'dotnet user-secrets' (dev) or an environment overlay (prod) — do not commit secrets. When blank, the connection editor's Advanced 'use my own OAuth client' fields are used instead.",
    "ClientId": "",
    "ClientSecret": ""
  }
}
