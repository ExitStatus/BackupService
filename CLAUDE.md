# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This is a **.NET 10 ASP.NET Core app (`Microsoft.NET.Sdk.Web`) hosting Blazor Server plus background worker services**, run as a **plain console executable in one of two modes** (foreground or detached background — see below; it is **no longer a Windows Service**). It started life as a Worker Service template and was converted: the same process serves an interactive Blazor Server UI *and* runs the scheduler/watchers as hosted background services.

## Commands

Run from the repo root (`C:\Projects\BackupService`). The solution is `BackupService.slnx` (the newer XML solution format); the project lives in `BackupService/`.

```powershell
dotnet build                          # build the solution
dotnet run --project BackupService    # run web host + worker (Ctrl+C to stop)
dotnet watch --project BackupService run   # run with hot reload
```

Running interactively (console / `dotnet run`) starts Kestrel and serves the Blazor UI. To run the deployed exe, use one of the two run modes (see "Run modes" below).

**HTTP only — deployed on `http://localhost:55000`, dev on `http://localhost:5080`.** There is no HTTPS (the admin UI is local-only). The port lives in **environment-specific** config (not base `appsettings.json`): `appsettings.Production.json` → `Kestrel:Endpoints:Http:Url` = 55000 (a deployed run has `ASPNETCORE_ENVIRONMENT` unset → Production), `appsettings.Development.json` → 5080. Base `appsettings.json` intentionally has **no** `Kestrel:Endpoints` — otherwise Visual Studio reads it as the default and launches the browser on 55000 even in debug. The dev launch profile (`launchSettings.json`) sets `ASPNETCORE_ENVIRONMENT=Development` (web-standard) and `applicationUrl=5080`. Note a `Kestrel:Endpoints` value takes precedence over `ASPNETCORE_URLS`/`applicationUrl`, so the per-environment port is set via the config layer.

**Run modes (`Hosting/`):** the exe parses its command line (`ApplicationModeParser`) into one of three behaviours:
- **no args → foreground:** runs the host attached to the terminal; all `ILogger`/console output goes to the terminal, Ctrl+C stops it.
- **`-background` / `-bg` → background:** relaunches the exe as a **detached `--worker` child** (`BackgroundProcessManager.LaunchDetached` — `CreateNoWindow`, working dir = exe dir) and returns immediately printing the child PID. The child has no terminal, so it points `Console.Out`/`Console.Error` at a **`DailyRollingLogWriter`** that writes `{data dir}\logs\backupservice-{yyyy-MM-dd}.log` (one file per local day, auto-flushed) with **14-day retention** (purged on start + each daily roll; `SelectExpiredLogFiles` is the pure, unit-tested core).
- **`-stop` → stop:** sets a named stop event (`BackgroundProcessManager.RequestStop`) that the running host waits on (registered via `RegisterStopSignal(app.Lifetime)` → `IHostApplicationLifetime.StopApplication()`), then waits on the PID file for the process to exit. A no-op stop (nothing running) is not an error.

A **single-instance guard** (a per-session `Local\` named `Mutex` in `BackgroundProcessManager`) makes a second launch (foreground or background) exit with "Backup Service is already running." instead of a Kestrel bind failure; the names are per-session, so each interactive Windows user runs an independent instance. The host pins **`ContentRootPath = AppContext.BaseDirectory`** (the old `UseWindowsService()` used to do this) so `appsettings.json`/the Kestrel binding are found regardless of the launching working directory. A `{data dir}\backupservice.pid` file is written on start and deleted on graceful exit. **Future (not built):** a Settings option to start the app on Windows logon (it would register the exe with `-background`).

**Deploy via publish profile:** `Properties/PublishProfiles/Install.pubxml` publishes to `C:\Tools\BackupService` — `dotnet publish -p:PublishProfile=Install`. A **pre-publish** target runs `BackupService.exe -stop` (ignored if nothing is running) so files aren't locked during the copy; there is **no** service-install step. After publishing, start it with `C:\Tools\BackupService\BackupService.exe -background` (stop with `-stop`). **One-time migration:** a build deployed before this change ran as a Windows Service — remove that leftover service once with an elevated `BackupService.exe --uninstall` from the **old** exe (or via `services.msc`). If the UI ever needs network/HTTPS access, configure a real cert under `Kestrel:Endpoints` and add `CookieSecurePolicy.Always` + `UseHttpsRedirection()`.

Tests live in the sibling **`BackupService.UnitTests`** project (in the solution). Stack: **NUnit** + **Moq** + **FluentAssertions**. Note FluentAssertions is pinned to **7.x** (the last Apache-2.0/free line — 8.x requires a paid commercial license); don't bump to 8.x without a license.

```powershell
dotnet test                                                   # run all tests
dotnet test --filter "FullyQualifiedName~TestClassName.TestMethodName"   # single test
```

Testing services that use the DbContext factory: mock `IDatabaseContextFactory` with Moq and have `CreateDbContext()` return a `BackupDbContext` over a **shared in-memory SQLite** connection (`new SqliteConnection("DataSource=:memory:")` kept open for the test, `EnsureCreated()` for schema) — see `Authentication/AdminCredentialServiceTests.cs` for the pattern. Use `NullLogger<T>.Instance` for `ILogger` dependencies.

### Database / EF Core migrations

Migrations are kept under `BackupService/Database/Migrations`. Always pass `--output-dir Database/Migrations` when adding one so it lands there rather than the default `Migrations/` folder. The `dotnet-ef` tool version must match the EF Core packages (currently 10.0.9).

```powershell
dotnet ef migrations add <Name> --project BackupService --output-dir Database/Migrations
dotnet ef database update --project BackupService     # apply migrations manually
dotnet ef migrations remove --project BackupService   # undo the last (unapplied) migration
```

The app also calls `db.Database.Migrate()` on startup, so pending migrations apply automatically when it runs. The SQLite file (`backupservice.db`, plus `-wal`/`-shm`) is generated in the **data directory** (`BackupDatabaseLocation` — per-user `%LOCALAPPDATA%\BackupService` for a deployed run, the `bin\…` output in Development) and is not source.

## Architecture

- `Program.cs` — host bootstrap via `WebApplication.CreateBuilder`. First dispatches the run mode (`-stop`/`-background`/foreground+`--worker`; see "Run modes" above) and acquires the single-instance lock, then builds the host with an explicit `ContentRootPath = AppContext.BaseDirectory`, registers the DB factory, cookie auth + authorization, Razor Components with interactive server rendering (`AddRazorComponents().AddInteractiveServerComponents()`), registers the backup scheduler as the hosted service, then maps the Blazor endpoint (`MapRazorComponents<App>().AddInteractiveServerRenderMode()`) and a `/logout` POST endpoint. The middleware pipeline is `UseAuthentication` → `UseAuthorization` → `UseAntiforgery` → `MapStaticAssets` → endpoints. Startup also runs `db.Database.Migrate()` and `IAdminCredentialService.EnsureSeededAsync()`. **Static assets use .NET fingerprinting:** `app.MapStaticAssets()` (not `UseStaticFiles`) serves `wwwroot`, and `App.razor` links the stylesheet via `@Assets["app.css"]` so the URL carries a content hash — CSS/JS edits bust the browser cache automatically (avoids the stale-CSS problem where new markup loads but old styles persist).
- **Backup execution is driven by the scheduler, not a `Worker`.** (`Worker.cs` — the old idle heartbeat placeholder — has been removed; `BackupSchedulerService` is the `BackgroundService` now.) Backup logic belongs in the per-type handlers in `Scheduling/` (see below), not inline in the host.
- `Database/` — **all** database code lives here: `BackupDbContext` (EF Core, SQLite), the entity models, the `Migrations/` subfolder, `BackupDatabaseLocation`, and the `DatabaseContextFactory`. Keep new entities and DbContext changes in this folder. Core domain: a `Profile` has one-to-many `FolderPair` records (cascade delete); the `FolderPair` carries status enums (`FolderPairStatus`, `FolderPairLastRunStatus`). The second profile type, **`ProfileType.InstantSync`**, has its own one-to-many child, **`InstantSyncItem`** (cascade delete) — each with `Name`, `SourceFolder`, `TargetFolder`, an `int DebounceMilliseconds`, `IncludeSubFolders` and `AllowDeletions` (no status/overwrite columns — instant sync is source-authoritative and has no discrete run). The third profile type, **`ProfileType.ArchiveSync`**, has its own one-to-many child, **`ArchiveSyncItem`** (cascade delete) — each with `Name`, `SourceFolder`, `TargetFolder`, a `FileName` (the base archive name a timestamp is appended to), `IncludeSubFolders`, a `bool OnlyCopyOnChange` (when true, only create a new archive when the source content changed since the most recent one — see the fingerprint mechanism in `ArchiveSyncProcessor`), an `ArchiveCompressionLevel CompressionLevel` (Optimal/Fastest/SmallestSize/NoCompression — a project enum mapped to the BCL `CompressionLevel` via `ArchiveCompressionLevelExtensions.ToCompressionLevel()`; defaults Optimal), a `bool PasswordProtect` + a `string? PasswordEncrypted` (the archive password, **encrypted at rest** via `ISecretProtector` — same scheme as SMB; never stored/logged in plaintext) + an `ArchiveEncryptionMethod EncryptionMethod` (`Aes256`/`ZipCrypto`), an `ArchiveRetentionMode RetentionMode` (`KeepLastN`/`GrandfatherFatherSon`), an `int RetentionCount` (N — total for keep-last-N, per-level for GFS), an `int MaxLevels` (GFS level count; 1 otherwise) and an `int RunCount` (monotonic count of archives created — the only mutable run-state, drives the GFS promotion cadence; no Status/LastRunStatus columns). ArchiveSync is **scheduled** (uses `Profile.Schedule`/the scheduler, like FolderPair — not watcher-driven). The fourth profile type, **`ProfileType.LightroomArchive`**, has its own one-to-many child, **`LightroomArchiveItem`** (cascade delete) — like `InstantSyncItem` (`Name`, `SourceFolder`, `TargetFolder`, `DebounceMilliseconds`, `IncludeSubFolders`, `AllowDeletions`) but with **only a connectable target** (the target connection is the profile-level `Profile.TargetConnectionId`; the source is local-only, since it's watcher-driven). It is **watcher-driven** like InstantSync, plus a per-copy raw-sidecar step: whenever a file is copied, its matching raw originals are pulled from a Lightroom catalog into a RAW folder beside the copy. Its three settings are **profile-level** (shared by all items) and live on `Profile` itself (the one type whose config isn't entirely on the child): `string? LightroomFolder` (the local catalog scanned recursively), `string? RawFormats` (CSV of extensions, e.g. `.DNG,.ARW`) and `string? RawFolderName` (the RAW sub-folder name, default `RAW`) — see `LightroomArchiveSettings` + `LightroomArchiveProcessor` in `Scheduling/`. A profile uses the child collection matching its `Type` (`FolderPairs`, `InstantSyncItems`, `ArchiveSyncItems` or `LightroomArchiveItems`); the others stay empty. **`FolderPair` and `ArchiveSyncItem` each own a one-to-many `Filters` collection** (`FolderPairFilter` / `ArchiveSyncFilter`, cascade-deleted via the default required-FK convention) — include/exclude rules each with a `FilterDirection Direction` (Include/Exclude), a `FilterKind Kind` (File/Folder/**Path**) and a `Pattern`. File/Folder are **name-only wildcards** (matched in every sub-folder); **`Path` (exclude only)** is a literal path *relative to the source root*, matched against the exact location (that file, or that folder and its whole subtree) — not by name. Empty includes = back up everything; see `BackupFilter` in `FileSystem/`. **A profile's own run status (`ProfileStatus` — Idle/Running/Error) is deliberately NOT persisted** — it describes the current run and is tracked in memory by `IProfileStatusService` (see `Profiles/`); there is no `Profile.Status` column. Each `FolderPair` also has an `OverwriteBehaviour` enum (how to treat an existing/newer file at the destination). `Profile` has a `bool Enabled` (on/off switch) that **defaults to true** via a store default — `BackupDbContext.OnModelCreating` configures `Property(p => p.Enabled).HasDefaultValue(true)` so the migration backfills existing rows to enabled; the C# initializer is also `= true`. (EF Core 7+ then treats `true` as the sentinel, so a `false` value is still persisted — disabling a profile works.) `Profile` also carries **per-profile notification settings** — `bool NotificationsEnabled` (master switch, store default true), `bool NotifyOnStart` (default false) and `bool NotifyOnComplete` (store default true) — edited on the **Notifications** tab of `ProfileDialog` and read by the backup handlers (see `Notifications/`); `NotificationsEnabled`/`NotifyOnComplete` use the same `HasDefaultValue(true)` backfill as `Enabled`. A separate `bool ShowProgressWindow` (default false) on the same tab opts the profile into an on-screen progress window while it runs (Windows-only; rendered by `Notifications/WindowsProgressWindowService`). `Profile` also has a nullable `DateTimeOffset? DateNextRun` (the next scheduled run time, written by the scheduler — see `Scheduling/`) and a `bool HandleMissedSync` (when true, a scheduled profile that missed its run while the service was down runs immediately on the next startup; only meaningful for scheduled types). Other tables: `AdminCredential`, `AuthenticationHistory`, `BackupRecord` (a placeholder, **never written**), **`BackupRun`** (structured run history for the dashboard — one row per discrete scheduled/manual run, covering **both** backup runs and **scheduled-task** runs, distinguished by a `RunKind Kind` (Backup/ScheduledTask): a **nullable** `int? ProfileId` FK (cascade-delete; set for a backup run) **and** a nullable `int? ScheduledTaskId` FK (cascade-delete; set for a scheduled-task run), denormalised `ProfileType Type` (unused for scheduled-task runs), `StartedUtc`, `long DurationMs`, `RunOutcome Outcome` (Success/**CompletedWithWarnings**/CompletedWithErrors/Failed), `Copied`/`Updated`/`Deleted`/`Errors`/**`Warnings`**/**`long BytesCopied`** (total bytes written; all zero for a scheduled-task run), `bool Manual`, and a loose `int? OperationLogId` for drill-through; written by `IBackupRunRecorder` — `RecordAsync` from each backup handler, `RecordScheduledTaskAsync` from the scheduled-task runner — see `Scheduling/`/`Dashboard/`), **`ScheduledTask`** + its one-to-many **`ScheduledTaskStep`** (cascade delete — the Scheduled Tasks feature; a cron-scheduled task that runs one or more ordered OS-command steps. `ScheduledTask`: `Name`, `Description?`, `bool Enabled` (default true), `string? Schedule` (cron), `DateTimeOffset? DateNextRun`, `bool HandleMissedSync`, `DateLastRun`, `DateCreated` — mirrors `Profile`'s scheduled fields but is **not** a profile. `ScheduledTaskStep`: `int Order` (ascending = run order), `string? Name`, a `ScheduledTaskStepKind Kind` (**Command** or **PowerShell**), and the per-kind fields — Command kind uses `string? Command` (executable path, or full command line when `RunViaShell`), `string? Arguments` (ignored when `RunViaShell`) and `bool RunViaShell`; PowerShell kind uses `string? Script` (the inline script body) — plus a shared `string? WorkingDirectory`. See the `ScheduledTasks/` + `Scheduling/ScheduledTasks/` folders), and `LogRetentionSettings` (a **single-row** settings table — `AuthenticationLogRetentionDays` default 7, `OperationLogRetentionDays` default 30 — seeded lazily by `ILogRetentionService`; drives the log-purge below). There is also an operation-logging pair: `OperationLog` (header — unbounded `Name`, `TimestampUtc`, and an `OperationLogLevel` enum — Info/Warning/Error/Debug — the **severity of the operation as a whole**) has one-to-many `OperationLogDetail` rows (cascade delete; unbounded `Message`, `TimestampUtc`, an `int Sequence` for ordering lines within a log, and **its own `OperationLogLevel Level`** — each line carries its severity). **The header `Level` is derived from the detail lines, not set independently:** it is the most severe of its lines' levels — all Info/Debug → Info, any Warning → Warning, any Error → Error (Debug counts as Info for this) — maintained by `OperationLogger` as lines are appended (a header only ever rises; see `Logging/`). A detail-less log keeps the level it was created with. `OperationLog` also has a **nullable** `int? ProfileId` FK to `Profile` (null for logs not tied to a profile) configured in `OnModelCreating` with `OnDelete(DeleteBehavior.Cascade)` — deleting a profile cascade-deletes its logs (cascade is the non-default behaviour for a nullable FK, so it's set explicitly). Note the profile *deletion* log is deliberately left unassociated (`ProfileId` null) so it survives the delete. Written via the `Logging/` factory; read/displayed by the Logs sidebar section.
  - **Factory pattern — the `DbContext` is NOT registered in DI.** Instead `IDatabaseContextFactory` (impl `DatabaseContextFactory`) is registered as a singleton. Code that needs the database injects `IDatabaseContextFactory` and calls `CreateDbContext()`, owning and disposing a short-lived context per unit of work (`using var db = factory.CreateDbContext();`). Do **not** add `AddDbContext<BackupDbContext>` back. `DatabaseContextFactory` also implements `IDesignTimeDbContextFactory<BackupDbContext>` so `dotnet ef` can build a context at design time without the context being in DI.
  - **SQLite caveat:** SQLite cannot `ORDER BY` (or compare) a `DateTimeOffset` column. For "newest first" ordering use the autoincrement `Id` (monotonic with insertion) rather than a timestamp column — see `AuthenticationHistoryService.GetPageAsync`. `DateTimeOffset` columns are fine to store and `SELECT`/display.
  - **Database location is per-user** (`BackupDatabaseLocation.GetConnectionString()`): one SQLite file per user. A deployed run (any non-`Development` environment) puts it (and the data-protection key ring, the `logs\` folder and the PID file) under **`%LOCALAPPDATA%\BackupService`** — `Environment.SpecialFolder.LocalApplicationData`, *not* Roaming, since a SQLite file must not roam — so multiple Windows users each get an isolated config; a `Development` run keeps it next to the build output (`AppContext.BaseDirectory`, the `bin\…` folder) for the dev workflow. The decision reads `ASPNETCORE_ENVIRONMENT` directly (it runs before the host is built). There is no connection string in `appsettings.json` — it is computed in code. The key ring is **not** migrated — the old service's LocalSystem-DPAPI key ring isn't decryptable by a normal user — so SMB/Google-Drive passwords carried in the moved DB are unreadable and must be **re-entered** (already the supported `ConnectionSecretMigrator` "logged for re-entry" path).
- `Components/` — the Blazor Server UI. `App.razor` is the root HTML document (loads `_framework/blazor.web.js` and the theme `app.css`), `Routes.razor` wires the router via `AuthorizeRouteView` (unauthenticated → `RedirectToLogin`), and routable pages live in `Components/Pages/` (`@page` directives). `_Imports.razor` holds shared `@using`s for the component tree. Two layouts: `Components/Layout/MainLayout.razor` (default — top bar with signed-in user + Log out, gated by `<AuthorizeView>`, content in `.page`) and `Components/Layout/AuthLayout.razor` (full-screen centered, used by `Login` via `@layout AuthLayout`).
- **Layout shell:** `MainLayout` wraps the top bar + `<main class="app-main">` in `.app-shell` (a flex column at `height: 100vh`), so `.app-main` fills the viewport below the title bar. Pages that want a centred, max-width look opt into a `.page` wrapper; pages that need full height/bleed use `.app-main` directly.
- **Workspace pages (sidebar + content):** both the home **`BackupServicePage`** (route `/`, title "Backup Service") and the **Settings** page use the shared `.workspace` / `.sidebar` / `.sidebar-item` / `.workspace-content` CSS: a left-docked navigation sidebar (a self-contained control owning its items, two-way `@bind-SelectedKey`) plus a content area that swaps the selected section's control. `BackupServicePage` has `BackupServiceSidebar` (items: Dashboard → `Dashboard.razor`, Backup Profiles → `Profiles.razor`, **Scheduled Tasks → `ScheduledTasksControl.razor`**, Connections → `ConnectionsControl.razor`, Logs → `Logs.razor`); the **Dashboard** section renders run statistics + **ApexCharts** (see the `Dashboard/` bullet); the **Scheduled Tasks** section is a Profiles-style grid of cron-scheduled command tasks (see the `ScheduledTasks/` bullet); Settings has `SettingsSidepanel` (items: Authentication → `AuthenticationPanel`, Logging → `LoggingPanel` (log retention settings, `Components/Pages/Logging/`), **Options → `OptionsPanel`** (desktop-integration toggles, `Components/Pages/Options/`)). Add a section = one sidebar `NavItem` (key/label/icon) + a matching `case` arm + a panel control. The **Logs** section is a paged table of `OperationLog` headers (newest first, via `IOperationLogService`) — each row shows the header's level as a coloured `.log-level` pill, the associated **Profile** name (or a muted `—` when not tied to one), the log name, and timestamp, plus a `+`/`−` button that expands a **terminal-style view** of that log's `OperationLogDetail` lines — monospace `[{level}] [{timestamp}] {message}` rows (no wrap, like a real terminal; long messages scroll horizontally), each line tinted by its own `Level` (Warning → amber, Error → red, Info/Debug → default), lazy-loaded on first expand and cached. A `message` may itself contain **CRLFs** (a consolidated single row whose lines are split by `\r\n` — e.g. the ArchiveSync "Archived {n} file(s):" list); `.log-terminal-line` is `white-space: pre` with `min-height` (not a fixed height) so such a row grows to multiple visual lines. Above the terminal view sits a per-log `.log-detail-filter` bar — a free-text line filter plus two level checkboxes, **Warning ({n})** and **Error ({n})** (each labelled with the count of cached lines at that level) — that narrows the displayed lines **client-side** from the cached set (the free-text input is wrapped in `.filter-clearable`, which shows a clear `&times;` button when it has content — the same wrapper/`.filter-clear` pattern used by the header name filter). The two checkboxes default **both unchecked** = show every line; ticking either shows **only** those levels and ticking both shows Warning+Error (an additive OR, not the single-select dropdown the header bar uses). Each expanded log keeps its own filter state, keyed by log id, in `_detailFilters` (text) + the `_detailWarning`/`_detailError` id sets; `FilteredDetails(logId)` applies a case-insensitive `Message` `Contains` then the Warning/Error level filter before feeding `Virtualize`. Because a big backup can write thousands of lines, the lines are rendered with `<Virtualize>` (only visible rows are in the DOM) inside a `.log-terminal` box capped at ~20 lines (`max-height` = 20 × the 20px `.log-terminal-line` height, matched by `LineHeight`/`ItemSize` in `Logs.razor.cs`) that scrolls vertically beyond that. The detail cache is a `List<OperationLogDetail>` so it can feed `Virtualize.Items`. The expand control only shows when the log actually has detail lines — `GetPageAsync` populates a non-persisted `OperationLog.DetailCount` ([NotMapped], via a grouped count query for the page) and the grid hides the `+` when it's 0, so self-describing logs (message-in-name, no sub-entries) render as a plain row. Above the table a `.filter-bar` has a free-text name filter (live, `@oninput`), an **Include Messages** checkbox, a level `Dropdown` (`OperationLogLevel?`, null = "All levels"), and a profile `Dropdown` (`int?` profile id, null = "All profiles"; options come from `IProfileService.GetSummariesAsync`). `GetPageAsync(page, size, filter, includeMessages, level, profileId)` does the matching server-side — name (and detail `Message` too when the box is ticked) via `EF.Functions.Like` (LIKE is case-insensitive for ASCII in SQLite), an exact `Level` match, and an exact `ProfileId` match; all filters combine with AND, and each returned log has its `Profile` navigation `Include`d for the column.
- **Naming caveat:** never name a type or page folder `BackupService` — it equals the root namespace and shadows it (breaks `@using BackupService...`). The home page folder/class is `BackupServicePage` for this reason.
- **Interactivity:** pages are static SSR by default. Components needing interactivity opt in with `@rendermode InteractiveServer` (infrastructure already wired in `Program.cs`). The Settings page is the first such page (the `SettingsSidepanel` nav updates the shown section via `@bind-SelectedKey`). Keep the login page static SSR (it must write the auth cookie on its form POST).
- **Theme/styling:** a single global dark theme in `wwwroot/app.css`, driven by CSS custom properties under `:root` (`--bg`, `--surface`, `--accent`, …) — re-skin by editing those variables. Shared classes: `.topbar`, `.page`, `.card`, `.auth-shell`/`.auth-card`, `.btn-secondary`, `.alert-error`. No CSS framework (no Bootstrap). Note: `wwwroot` is served via `app.MapStaticAssets()` (the Web SDK static-web-assets system with fingerprinting) — live from source in Development and copied into `wwwroot` on `publish`. Reference the stylesheet via `@Assets["app.css"]` (already done in `App.razor`) so the served URL is content-hashed; a plain `href="app.css"` caches indefinitely and shows stale styles after edits. (A raw `dotnet bin\...dll` run in Production still can't serve `/app.css` because the assets aren't copied to `bin/` on a plain `build` — a test artifact only, not a real-deployment issue.)
- `Components/Controls/` — shared, reusable custom controls (namespace `BackupService.Components.Controls`, imported globally via `Components/_Imports.razor`). **Modal dialogs are not here — they live in `Components/Dialogs/` (see below).** `Notification` — a non-modal toast: call `Show(message, NotificationLevel)` via `@ref`; shows bottom-right for 8s then fades (Success/Warning/Error → green/amber/red). `Dropdown<TValue>` — theme-controlled dropdown that **replaces the native `<select>`** (whose OS-drawn popup ignores the dark theme on Windows — do not use raw `<select>`; use this). Supports `@bind-Value`, takes `Items` + a `DisplayText` func, optional `Disabled`; closes on option/backdrop click. `ToggleButton` — a sliding on/off switch (knob left = `false`, right = `true`); two-way bindable via `@bind-Value` (bool), optional `Disabled`. `ProfileEnabledControl` — composes a `ToggleButton` with a status swatch (green when true, red when false); `@bind-Value` + raises `ValueChanged` when toggled (used in the Profiles grid's Enabled column to persist inline via `IProfileService.SetEnabledAsync`). `TimePicker` — themed **clock-face time picker** (Material-style) replacing the native `<input type="time">` (whose OS popup ignores the dark theme): a trigger shows the current time and opens a popup with a 12-hour clock you click to set the hour (which auto-advances to the minute), an AM/PM toggle, and a clickable HH:MM header to switch between setting hour/minute; two-way bindable via `@bind-Value` (a `TimeOnly`), applies on OK. Used by `ScheduleDialog` for the Daily/Weekly/Monthly "At time" field. Clock geometry constants in the code-behind must match the `.tp-clock` size in `app.css`; clicks are read from `MouseEventArgs.OffsetX/Y` so all overlay children (numbers/hands/knob) are `pointer-events: none`. `FolderPairControl` — manages a profile's **list** of folder pairs: a table of pairs (shows the name — the source/target paths can be long) with per-row edit/delete and an Add button, bound to a `List<FolderPairModel>` it mutates; exposes a `Validate()` (requires ≥1 pair) the parent calls before submit. It opens `FolderPairEditDialog` (in `Components/Dialogs/`) to add/edit a pair. The `FolderPairModel` view model lives here alongside `FolderPairControl`. `InstantSyncControl` (+ its `InstantSyncItemModel`) is the InstantSync counterpart — same list/Add/Validate shape, opening `InstantSyncEditDialog`. `ArchiveSyncControl` (+ its `ArchiveSyncItemModel`) is the ArchiveSync counterpart, opening `ArchiveSyncEditDialog`. **The `FolderPairModel`/`ArchiveSyncItemModel` view models also carry `List<FilterEntryModel> Includes`/`Excludes`** (the include/exclude rules), which `Clone(...)` deep-copies (via `FilterEntryModelExtensions.Clone`) so an edit can be cancelled. `TabBar` — a small horizontal tab strip (the `SettingsSidepanel` section-switcher pattern, laid out horizontally): `Items` (key/label `TabItem` records) + two-way `@bind-SelectedKey`; the parent switches the shown content. Used by the tabbed `FolderPairEditDialog`/`ArchiveSyncEditDialog`. `FilterListEditor` — edits one tab of include/exclude entries (reused for both the Includes and Excludes tabs of both dialogs): a type radio (Include ⇒ "Specific file"/"Wildcard pattern"; Exclude ⇒ "File"/"Folder"/**"Path"**), a pattern box + Add, and a list with a derived type tag + remove. Takes `Entries` (this tab) + `OtherEntries` (other tab, for cross-tab clash) + `Direction` + an optional **`SourceFolder`** (the parent's source). When the Exclude type is **Path**, a **Browse** button appears that opens `FolderBrowserDialog` **rooted at `SourceFolder`** (`RootPath`, so you can't navigate above it) and stores the chosen folder as a path *relative* to the source via `Path.GetRelativePath` (a selection of the source root itself, or outside it, is rejected); a Path entry rejects wildcards. Browse is disabled until a source folder is set. Adds are gated by `FilterValidation`. The `FilterEntryModel` (`Id`/`FilterKind Kind`/`Pattern`) and the pure static `FilterValidation` (the three clash rules — duplicate on the same tab, same value on the other tab, literal already covered by an existing wildcard; returns an error string or null) live here too. **The "Specific file vs Wildcard" include distinction is not stored** — `Kind` is `File`/`Folder`/`Path`; whether a File entry is a wildcard is derived from whether its pattern contains `*`/`?`.
- `Components/Dialogs/` — **all modal dialog components** (namespace `BackupService.Components.Dialogs`, imported globally via `Components/_Imports.razor`). New dialogs go here, not in `Controls/` or a page folder. `FolderBrowserDialog` — modal server-side folder picker styled like the **Windows Explorer "Select Folder" dialog** (themed dark): window title bar, toolbar with back/forward/up + an address-bar breadcrumb (clickable path segments), a left navigation tree (Quick access + This PC → drives, lazy-expand), a details list with Name/Date modified/Type/Size columns (single-click selects + fills the Folder box, double-click drills in), and a bottom Folder textbox + Select Folder/Cancel. Uses `IFolderBrowser`; takes `InitialPath`, raises `OnSelect(string)`/`OnCancel`. The tree rows are a recursive `FolderTreeNode` control (lazy-loads children on expand). An optional **`RootPath`** confines browsing to that folder and its subtree: the sidebar shows only the root (not Quick access / This PC), Up stops at the root, the breadcrumb starts there, and a path outside it can't be navigated to or selected (`CanSelect`/`IsWithinRoot`) — used by `FilterListEditor`'s Path exclude. An optional **`AllowCreateFolder`** (default off) adds a **New folder** button to the toolbar that creates a sub-folder of the *currently open* folder via `IFolderBrowser.CreateDirectory` (inline name bar — auto-focused, empty by default, Enter creates — with invalid-char + duplicate checks, then re-lists and selects it) — **only the target-folder picker enables it** (each edit dialog passes `AllowCreateFolder="@(_browseFor == "target")"`; source and exclude-path browsing don't). Its Folder textbox (and the new-folder name box) carry `no-enter-submit` (see App.razor) so Enter doesn't submit the surrounding `EditForm`. `ScheduleDialog` — modal schedule builder producing a `ScheduleDefinition`. `ConfirmDialog` — reusable modal confirmation (`Title`/`Message`/`ConfirmText`, `OnConfirm`/`OnCancel`); confirm button uses the `.btn-danger` style. `FolderPairEditDialog` — a **3-tab** modal (`TabBar`: Detail / File Includes / Excludes) that adds/edits a single pair. **Detail** holds the existing fields (name + source/target with Browse + **Allow deletions** and **Include Sub-Folders** checkboxes + an **Overwrite behaviour** `Dropdown` whose selected value's `GetHelpText()` is shown underneath as `.help-text`); **File Includes** and **Excludes** each host a `FilterListEditor` over the model's `Includes`/`Excludes`. It hosts the folder browser and returns the edited `FolderPairModel` (the model + the list-level `FolderPairControl` stay in `Components/Controls/`). Save re-runs the Detail field validation and, on failure, switches back to the Detail tab. `InstantSyncEditDialog` — the InstantSync counterpart of `FolderPairEditDialog`: name + source/target with Browse + **Include Sub-Folders**/**Allow deletions** checkboxes, plus a **Debounce (seconds)** numeric field (edited in seconds, stored as `DebounceMilliseconds`), and **no** Overwrite dropdown (instant sync always overwrites). `ArchiveSyncEditDialog` — the ArchiveSync counterpart, a **5-tab** modal (Location / **Retention** / **Archive** / File Includes / Excludes): Location = name + source/target with Browse, **Include Sub-Folders** + **Only copy on change** checkboxes; the **Retention** tab holds a **Retention** mode `Dropdown` (`ArchiveRetentionMode`, with `GetHelpText()` blurb), a keep-count numeric field, and a **Maximum levels** numeric field shown only for grandfather-father-son; the **Archive** tab holds the **Archive file name** field (top), the **Compression** `Dropdown` (`ArchiveCompressionLevel`, with `GetHelpText()`) and the password options — a **Password protect** checkbox that reveals a password field (placeholder "leave blank to keep" when one is already stored) and an **Encryption** `Dropdown` (`ArchiveEncryptionMethod`, with `GetHelpText()`); a missing password on a newly-protected archive is validated and jumps to the Archive tab. The last two tabs host a `FilterListEditor` each (InstantSync has **no** filter tabs). `ProfileDialog` — create/edit a backup profile (hosts `ScheduleDialog` and the type-specific editor, e.g. `FolderPairControl`/`InstantSyncControl`/`ArchiveSyncControl`); **the Schedule field is disabled when the type is `InstantSync`** (it's watcher-driven, not scheduled — saved with a null cron; `FolderPair` and `ArchiveSync` are scheduled); see the `Profiles/` entry for its behaviour. `ChangePasswordDialog` — self-contained admin password-change modal (uses `IAdminCredentialService` + `IAuthenticationHistoryService`). The schedule is a profile-level field collected by `ProfileDialog` (which hosts the `ScheduleDialog`), not by `FolderPairEditDialog`.
- `Scheduling/` — `ScheduleMode` + `ScheduleDefinition` (the user-friendly schedule selection). `ToCron()` makes a standard 5-field cron string (stored in `Profile.Schedule`), `ToHumanReadable()` makes the label. **The raw cron is never shown to the user — always humanize.** `FromCron(cron)` parses the stored cron back into a definition (used to pre-fill the edit dialog's schedule builder) and `Describe(cron)` is the always-safe display helper ("Not scheduled" / friendly label / raw cron only as a last-resort fallback) used by the grid. Cron is validated/parsed with the **Cronos** package (also used for next-run times by the scheduler below).
  - **The scheduler that actually fires profiles** also lives here. `BackupSchedulerService` is a singleton registered in **three roles** — itself, `IBackupScheduler`, and the hosted `BackgroundService` (one shared instance via `AddSingleton(...)` + `AddSingleton<IBackupScheduler>(sp => sp.GetRequiredService<...>())` + `AddHostedService(sp => ...)`). On startup it loads every enabled profile that has a `Schedule` and registers it; a loop computes each profile's next run with Cronos (`CronExpression.GetNextOccurrence(DateTimeOffset.Now, TimeZoneInfo.Local)` — **local wall-clock, DST-correct**), waits until the soonest (capped at 1 min, and woken early on change via a `SemaphoreSlim`), then fires every due profile. **Jobs run concurrently** (each due profile launched on its own `Task.Run`, so a slow backup never blocks others) and a profile is **never run twice at once** — before spawning a thread the scheduler skips a profile whose run is still in progress (`IProfileStatusService.IsRunning`), and `BackupRunner` re-checks atomically via `TryBeginRun` (the authority). It reads profiles via `IDatabaseContextFactory` directly (not `IProfileService`) to avoid a DI cycle. The `IBackupScheduler` API is a single `SyncAsync(profileId)` that re-reads the profile and schedules it (enabled + parseable cron) or unschedules it (missing/disabled/blank/invalid) — `ProfileService` calls it after **every** mutation (`CreateAsync`/`UpdateAsync`/`SetEnabledAsync`/`DeleteAsync`) so edits take effect immediately. (`GetNextOccurrence(cron, from, tz)` is a pure static helper, extracted for tests.) **The scheduler is the single writer of `Profile.DateNextRun`** — it persists the computed next-run time (via `ExecuteUpdateAsync`, no load) whenever a schedule is registered (`SyncAsync`/startup), advanced after firing, or cleared on unschedule (null) — so the upcoming run is recorded for every scheduled profile and survives a restart. **Missed-run catch-up:** on startup `LoadAllAsync` reads each enabled scheduled profile's `HandleMissedSync` + its *previously persisted* `DateNextRun`; if `ShouldCatchUp(handleMissedSync, persistedNextRun, now)` (a pure static helper — true only when opted in **and** the recorded next-run is in the past, i.e. the service was down for it) it writes a **visible profile-associated operation log** ("Missed scheduled run for '{name}' (was due …) — running now on startup", via the injected `IOperationLogFactory`) so the catch-up is distinguishable in the Logs section, then fires the profile via the normal `FireJob` path (`manual: false`). Only one catch-up runs per startup however many occurrences were missed.
  - **What runs when a schedule fires is dispatched by `ProfileType`.** `IBackupRunner`/`BackupRunner` (singleton) is the dispatcher: `RunAsync(profileId, manual = false)` loads the profile **with its `FolderPairs`**, **skips the run if the profile is locked** (`IProfileStatusService.IsLocked` — an admin has it open in an edit/delete dialog), otherwise sets the in-memory status to `Running`, runs the handler, **stamps `DateLastRun` (the only DB write — status is no longer persisted) and *then* flips the status to `Idle`** (order matters: the grid reloads on the status change, so the timestamp must be saved first or the grid shows the previous run's `DateLastRun`). On failure the handler has already set `Error`; the runner stamps `DateLastRun` and re-asserts `Error` so the grid's status-triggered reload also picks up the fresh timestamp. It does **not** write its own operation log — **the handler owns the single per-run log** (see next). It calls the `IProfileTypeHandler` whose `Type` matches `profile.Type` (handlers injected as `IEnumerable<IProfileTypeHandler>`, indexed into a dictionary); an unmapped type is the one case the runner itself logs (a "Scheduled backup failed" error log) since no handler runs. On a handler exception the runner just records the failure via `ILogger` (the handler already set `Error` status and logged — see next). **A run is either scheduled or `manual`** — `manual: true` is a "Run now" from the Profiles grid (see `Profiles/`), routed through the *same* `RunAsync` so it goes through the identical status/locking/single-instance path; the only difference is the run's operation log is **prefixed `[Manual] `**. **`IBackupRunner.RequestStop(profileId)`** (the grid's Stop button) cancels an in-progress run: the runner wraps each run in a linked `CancellationTokenSource` registered per profile id, and `RequestStop` cancels it; the handler catches the `OperationCanceledException`, finishes its log as a **Warning** ("cancelled"), and the runner's `catch … when (cts.IsCancellationRequested)` returns the profile to **Idle** (not Error) to await its next scheduled run — the crash-safe copy deletes any partial temp on cancel, so nothing is left behind. `IProfileTypeHandler` is the per-type seam (`ProfileType Type { get; }` + `HandleAsync(Profile, manual, ct)`); `FolderPairHandler` (`Type => ProfileType.FolderPair`) and `InstantSyncHandler` (`Type => ProfileType.InstantSync` — a manual full reconcile, see the InstantSync bullet below) are the two so far. (`BackupRunner` `Include`s both `FolderPairs` and `InstantSyncItems` so whichever handler runs has its items.) It creates **exactly one** `OperationLog` per run: the header starts as `"{prefix}{Type.GetDescription()} Handler called with {n} folder pair(s)."` (e.g. "Folder Pairs Handler called with 2 folder pair(s).", or "[Manual] Folder Pairs Handler …" for a manual run), and a `finally` rewrites the header (via `SetSummaryAsync`) to `"{prefix}… ran successfully in {duration} — {c} copied, {u} updated, {d} deleted"` (Info) or `"{prefix}… completed with {n} error(s) in {duration} — …"` (Error when any file/folder op errored) or `"{prefix}… completed with {n} warning(s) in {duration} — …"` (**Warning** — errors==0 but files were skipped as a non-fatal warning, e.g. locked) or `"{prefix}… failed in {duration}"` (Error for a *catastrophic* failure outside the per-pair loop). Precedence: any error ⇒ "errors"; else any warning ⇒ "warnings"; else success. The "{ProfileType description} Handler" prefix is derived from the enum's `[Description]`, and the log already carries the profile via its FK. **Each handler also records one structured `BackupRun` row per run** via `IBackupRunRecorder`/`BackupRunRecorder` (singleton, DB factory; `Scheduling/`): in the same `finally`, before the `SetSummaryAsync` rewrite (so the row exists before the summary's `ILogWatcher.Notify` reaches the dashboard), it derives a `RunOutcome` (fatal → Failed; else errors>0 → CompletedWithErrors; else warnings>0 → CompletedWithWarnings; else Success) and inserts a row with the duration, the `BackupResult` counts (incl. `Warnings`), `manual`, and `log.OperationLogId`. This is the **only** structured run history (the `OperationLog` summary is free text); the dashboard reads it via `Dashboard/IDashboardService`. The continuous InstantSync/LightroomArchive watchers don't go through the handlers/runner, but **each flush that does real work records its own `BackupRun` row** (via the same `IBackupRunRecorder`, `manual: false`, guarded by `DeferredOperationLogger.WasCreated` so a no-op flush records nothing) — so live watcher activity shows in the dashboard's Recent Runs alongside scheduled/manual runs. **The actual file work is `IFolderPairSynchronizer`/`FolderPairSynchronizer`** (singleton; see `FileSystem/`/`Scheduling/`): for each pair the handler sets `FolderPair.Status = Running` (DB), appends a `"Folder pair '{Name}': {src} -> {tgt}"` line, calls `synchronizer.SyncAsync(pair, log, ct)`, aggregates the returned `BackupResult` (Copied/Updated/Deleted/Errors/**Warnings**/**BytesCopied** — a file that **couldn't be read** (locked by another process, *or* an unavailable cloud file such as a OneDrive files-on-demand placeholder a session-0 service can't hydrate) is a non-fatal Warning, not an Error; see `FileSystem/FileLock` — `IsSkippableReadError(ex, out reason)` classifies lock/sharing violations *and* the `ERROR_CLOUD_FILE_*` family (Win32 358–405) and yields the warning reason; each engine's crash-safe copy adds the written file's size to `BytesCopied` via `IBackupFileSystem.GetFileSize`), then persists `LastRunStatus = Success/Fail` + `Status = Idle`. A per-pair unexpected exception is logged and the run continues with the other pairs; only a catastrophic failure sets the in-memory `ProfileStatus.Error` and re-throws (per-file errors stay logged-but-non-fatal — the Error detail lines escalate the header to Error and the pair's `LastRunStatus` is `Fail`, but the run completes and the grid status stays Idle). **Add a profile type = one `IProfileTypeHandler` impl + its `AddSingleton<IProfileTypeHandler, …>()` registration** — no scheduler/runner change; each handler owns its single per-run log.
  - **The folder-pair sync engine** — `IFolderPairSynchronizer`/`FolderPairSynchronizer` (singleton) — resolves the pair's **source and target each to an `IBackupFileSystem`** via `IEndpointFileSystemFactory` (local, or a remote SMB connection per `FolderPair.SourceConnectionId`/`TargetConnectionId`; sessions disposed in a `finally`), then walks the source tree **one folder at a time** and mirrors it into the target — a copy **streams from the source filesystem into a crash-safe temp on the target filesystem** (`OpenRead`→`OpenWrite`→stamp timestamp→rename), so local↔SMB copies work either direction, logging **only operations actually performed** (no line for an unchanged file) to the run's `IOperationLogger` and returning a `BackupResult`. Per file, by `LastWriteTimeUtc` (**exact** UTC compare): destination missing or source newer → copy; equal → skip; destination newer → the pair's `OverwriteBehaviour` (`DoNotOverwriteNewer` skip / `AlwaysOverwrite` overwrite / `UpdateOnlyIfContentMatches` → if byte-equal, sync the destination's timestamp to the source's, else skip). `IncludeSubFolders` → depth-first recursion; `AllowDeletions` → **full mirror** (delete orphan target files *and* orphan sub-folders recursively). **Copies are crash-safe:** every copy/overwrite is written to a dot-prefixed `.{name}.tmp` in the target folder via `CopyThroughTempAsync` (the deterministic name comes from `CrashSafeTempName`), then (on success) the original is deleted and the temp renamed onto it; on **any** error the temp is removed (best-effort) so a partial/temp file is never left behind. **A leftover temp from a previously interrupted run** (e.g. the machine hibernated mid-copy, so cleanup couldn't run) is swept at the start of each target folder's processing: a `.{name}.tmp` (recognised by `IsCrashSafeTempName`) that isn't itself a source file is deleted regardless of `AllowDeletions` and excluded from the rest of that folder's pass — a genuine source file matching the pattern is protected by the source-name check. A file write/delete failure or a folder-access failure is logged as an **Error** line with the reason and counts toward `Errors` (the run continues). All filesystem access goes through **`FileSystem/IBackupFileSystem`/`BackupFileSystem`** (singleton; a thin `System.IO` wrapper) so the engine's rules and cleanup-on-error are unit-tested against a fake (`FolderPairSynchronizerTests`).
  - **InstantSync (the second profile type) is watcher-driven, not scheduled.** It has its own background driver, **`InstantSyncWatcherService`** (singleton in **three roles** like `BackupSchedulerService` — itself, `IInstantSyncManager`, hosted `BackgroundService`). On startup it attaches a `FileSystemWatcher` to each item of every **enabled** `InstantSync` profile; **`IInstantSyncManager.SyncAsync(profileId)`** (the watcher counterpart of `IBackupScheduler.SyncAsync`) re-reads the profile and (re)attaches or tears down its watchers — **`ProfileService` calls both `scheduler.SyncAsync` and `instantSyncManager.SyncAsync` after every mutation** (each is a no-op for the wrong type), so enabling/disabling/editing/deleting an InstantSync profile starts/stops its watchers immediately. Per item, change events are **debounced** (`DebounceMilliseconds`): events accumulate in pending change/delete sets behind a gate lock, a timer is re-armed on every event, and when it fires after the quiet window the batch is processed on a **single in-flight pass** (a `_processing` flag + a re-arm in the `finally` guarantees no lost events and no overlapping pass — the "handle incoming changes while copying" requirement). Each flush owns **one** profile-associated `OperationLog` (`"Instant Sync '{item}' — {n} change(s)"` rewritten to a summary, exactly like the handler pattern) — but via a **`Logging/DeferredOperationLogger`** so a flush that does no real work writes **no log at all**. (The `{n}` change count is the number of distinct queued path-events, not files copied; a build touching `bin` produces many directory touch-events and short-lived temp files that are no-ops at flush time — directory events whose target already exists, or files that vanished before the debounce settled — so without deferral they'd show up as noisy "synced N change(s) — 0 copied" entries.) The actual copy/delete is **`IInstantSyncProcessor`/`InstantSyncProcessor`** (singleton over `IBackupFileSystem`, unit-tested via `InstantSyncProcessorTests`): each changed source path is rebased under the target and copied **crash-safe** (the same dot-`.tmp`→rename idiom as `FolderPairSynchronizer`; the helper is duplicated, not shared); deleted source paths are mirrored only when `AllowDeletions`. InstantSync is **source-authoritative** (a changed file always overwrites the target — no `OverwriteBehaviour`). The manual **"Run now"** path reuses the runner/handler seam: `InstantSyncHandler` does a one-off full reconcile by mapping each `InstantSyncItem` onto a transient `FolderPair` (`OverwriteBehaviour.AlwaysOverwrite`) and calling the existing `IFolderPairSynchronizer` — so the grid's Run-now works unchanged. **Add a watcher-driven profile type = its own manager/processor/handler in this shape**, registered alongside these.
  - **ArchiveSync (the third profile type) is scheduled** (like FolderPair — it fires from `BackupSchedulerService`, no watcher). Its handler is **`ArchiveSyncHandler`** (`Scheduling/ArchiveSync/`, `Type => ProfileType.ArchiveSync`), same shape as `FolderPairHandler`: one per-run `OperationLog` (`"[Manual] "`-prefixed for Run-now) rewritten in a `finally` to a summary (`"… ran successfully in {d} — {n} archive(s) created, {m} pruned"` / `"… completed with {e} error(s) …"` / `"… failed in {d}"`), per-item try/catch so one item's failure doesn't abort the rest. The actual work is **`IArchiveSyncProcessor`/`ArchiveSyncProcessor`** (singleton over `IBackupFileSystem`, unit-tested via `ArchiveSyncProcessorTests`): per item it (1) builds a ZIP of the source in a **local temp folder** (`IBackupFileSystem.GetTempFilePath` + `CreateZipFromDirectory`, which returns the entry names it added — these are **verbose-logged at `OperationLogLevel.Debug`** as a **single** detail row, `"Archived {n} file(s):"` followed by one file per **CRLF-separated** line, rather than thousands of rows; the file list can't change mid-zip and Debug counts as Info for the header; each entry is compressed at the item's `CompressionLevel`, and when `PasswordProtect` the processor decrypts `PasswordEncrypted` via `ISecretProtector` and passes it to `CreateZipFromDirectory` to **encrypt** the archive (AES-256 or ZipCrypto per `EncryptionMethod`) — a protected item with no/undecryptable stored password errors rather than producing an unencrypted zip), (2) **crash-safe copies** it into the target as `{FileName}_{yyyy-MM-dd_HHmmss}.zip` (the same dot-`.tmp`→rename idiom, duplicated), then (3) applies **retention**. **"Only copy on change"** (`ArchiveSyncItem.OnlyCopyOnChange`): before zipping, `ComputeSourceFingerprint` builds a **manifest** of every archived file's SHA256 (one ordinal-sorted `{entry}\t{hash}` line per file, matching the archived set's recursion + include/exclude filtering) into a local temp file, then hashes that manifest → a single source fingerprint. (A file that can't be read here — e.g. a OneDrive cloud-only placeholder — is skipped from the manifest via `TryHashFile` rather than aborting the whole archive; the zip pass then flags it as a warning. See `FileSystem/FileLock`.) It looks at the **newest** existing target archive and reads its fingerprint from the ZIP's **archive comment** (`IBackupFileSystem.GetZipComment`); if it matches, the run is **skipped** (logged "Source unchanged … skipping", no zip, no copy, no retention, so `RunCount` doesn't advance). Otherwise the new fingerprint is stored in the created ZIP's comment (`CreateZipFromDirectory(…, comment)`). A null/unreadable comment always forces a rebuild — it never wrongly skips. The fingerprint runs against the **local** source (a remote source is already staged locally by this point). Timestamps are **local** wall-clock at second precision. **Retention modes** (`ArchiveRetentionMode`): `KeepLastN` deletes everything older than the newest N; **`GrandfatherFatherSon`** keeps N per level across `MaxLevels` levels — each archive's level is encoded in its name (`{FileName}_L{level}_{ts}.zip`) so retention is **derivable from the target folder listing** (a foreign/unparseable file is skipped, never deleted). The GFS algorithm, driven by the per-item **`RunCount`** (incremented only when an archive is actually created), on run index `k`: create the L1 "son"; for `L = 1..MaxLevels-1` (low→high) if `k % N^L == 0` **promote** (rename) the oldest at level `L` up to `L+1` (so a freshly-promoted father can cascade to grandfather the same run); then **trim** each level to N (deleting the aged-out, non-promoted archives; top-level overflow too). This gives true exponential spacing (L1≈last N runs, L2≈N², L3≈N³). The handler advances `RunCount` via a short-lived context after a successful create (mirrors `FolderPairHandler`'s per-pair status write). The manual **"Run now"** path is just `RunAsync(id, manual: true)` through the same runner/handler seam. **Add a scheduled profile type = its own handler (+ processor) in this shape**, registered alongside these.
  - **LightroomArchive (the fourth profile type) is watcher-driven** (like InstantSync, not scheduled), with an added raw-sidecar step. Its driver **`LightroomArchiveWatcherService`** (`Scheduling/LightroomArchive/`, singleton in three roles: itself, `ILightroomArchiveManager`, hosted `BackgroundService`) mirrors `InstantSyncWatcherService` — a debounced `FileSystemWatcher` per item, per-flush `DeferredOperationLogger` so no-op flushes write nothing — and `ProfileService` calls `lightroomArchiveManager.SyncAsync(id)` after every mutation (alongside `scheduler`/`instantSyncManager`). The engine **`ILightroomArchiveProcessor`/`LightroomArchiveProcessor`** (singleton; unit-tested via `LightroomArchiveProcessorTests`) is **endpoint-aware** — modelled on `FolderPairSynchronizer`'s cross-filesystem copy, *not* the local-only `InstantSyncProcessor`: it injects `IEndpointFileSystemFactory` (resolve the target, local or a connection) + the local `IBackupFileSystem` (source + Lightroom catalog are always local), so the watcher has **no local/remote split** — it always calls `ProcessBatchAsync` regardless of target. Per changed source path it crash-safe-copies the file across the two filesystems (copy-if-missing-or-changed by `LastWriteTimeUtc`, so redundant events/reconciles don't re-transfer), **then** looks up the file's basename in a Lightroom raw **index** (basename → local raw paths, built **once per batch** from a recursive walk of `LightroomFolder`, matching the profile-level `RawFormats`, case-insensitive) and crash-safe-copies each match into a `{RawFolderName}` sub-folder **beside the copied file** (`{dir of rebased target}\{RawFolderName}\{raw}`). Deletions (only when `AllowDeletions`) mirror the rebased target *and* any matching raw(s) in that sibling RAW folder. The profile-level settings reach it via **`LightroomArchiveSettings`** (`FromProfile`: `LightroomFolder`, the parsed raw-extension set, `RawFolderName`; defaults `.DNG,.ARW`/`RAW`). The manual **"Run now"** path is **`LightroomArchiveHandler`** (`Type => ProfileType.LightroomArchive`, mirrors `InstantSyncHandler`: one `[Manual]`-prefixed per-run `OperationLog`, one `BackupRun` row): a **full reconcile** enumerating every in-scope source file per item (via the injected local `IBackupFileSystem`) and feeding them to `ProcessBatchAsync` as the changed set (no deletes). Like InstantSync, both the manual run **and each continuous watcher flush that does real work** record a `BackupRun` row (the watcher passes `manual: false`, guarded by `WasCreated`), so live activity appears in the dashboard's Recent Runs. **`LightroomArchiveItemService`/`ILightroomArchiveItemService`** (`Profiles/`, mirrors `InstantSyncItemService`) owns the item data; `ProfileService.CreateAsync`/`UpdateAsync` gained trailing optional params (`lightroomArchiveItems`, `lightroomFolder`, `rawFormats`, `rawFolderName`) and set the three profile-level fields.
- `FileSystem/` — `IFolderBrowser`/`FolderBrowser`: lists drives/subfolders on the server for the folder picker (admin-gated; inaccessible dirs skipped). Returns rich entries: `GetDrives()` → `DriveEntry(RootPath, Label)` (Explorer-style label e.g. `Windows (C:)`); `GetDirectories(path)` → `IReadOnlyList<FolderEntry>` (`FullPath`, `Name`, `DateModified`); `GetQuickAccess()` → the Desktop/Documents/Downloads/Pictures shortcuts that exist; `GetParent(path)`; and `CreateDirectory(parentPath, name)` → makes a sub-folder and returns its full path (used by the folder picker's New-folder button). Also `IBackupFileSystem`/`BackupFileSystem` (singleton) — a thin `System.IO`/`System.IO.Compression` wrapper (directory/file existence, list, copy, move, delete, timestamps, **`GetFileSize`** (for the bytes-copied stat), content-equal, plus `GetTempFilePath` for a unique local-temp path, `CreateZipFromDirectory` for archiving (its optional `comment` arg stores the ArchiveSync "only copy on change" fingerprint as the ZIP archive comment, `compressionLevel` sets how hard each entry is compressed, and a non-empty `password` encrypts the archive — AES-256 when `useAesEncryption` else legacy ZipCrypto. **The encrypted path uses SharpZipLib** (`ZipOutputStream`) since `System.IO.Compression` can't write encrypted zips; the unencrypted path stays on the BCL. `GetZipComment` reads the EOCD comment (still works on encrypted zips — the comment isn't encrypted)) and `GetZipComment` for reading that comment back (over SMB it tail-reads the EOCD record rather than downloading the whole archive)) used by the folder-pair sync engine (`Scheduling/FolderPairSynchronizer`), the instant-sync copy engine (`Scheduling/InstantSyncProcessor`) and the archive engine (`Scheduling/ArchiveSyncProcessor`) so their logic is testable against a fake; not unit-tested itself. **`IBackupFileSystem` also exposes `OpenRead`/`OpenWrite`** (stream primitives) so a copy can cross **two** filesystems (read from one, write to another). **`FileSystem/Smb/SmbBackupFileSystem`** implements the whole interface over a live SMBLibrary `ISMBFileStore` session (share-relative paths; streaming `SmbReadStream`/`SmbWriteStream`; the local-only `GetTempFilePath`/`CreateZipFromDirectory` throw `NotSupportedException`). **`FileSystem/IEndpointFileSystemFactory`/`EndpointFileSystemFactory`** resolves a folder-pair side — a `int?` connection id + configured path — to an `EndpointFileSystem(FileSystem, BasePath, Session)`: null id → the shared local `BackupFileSystem`; a set id → a connected `SmbBackupFileSystem` (via `IConnectionResolver`) rooted at the connection's `RootFolder`. The session is disposed after the run. *(SMB read/write is integration-only — not unit-tested; the engine's local↔remote logic is covered by a fake-filesystem cross-fs test.)* **In-use/locked files are handled, not fatal:** every source read uses a **permissive share mode** (`FileShare.ReadWrite | FileShare.Delete`) so a file another process holds open *for writing* (a log, a VS `.vsidx` index, …) can still be read — plain `File.Copy`/`CreateEntryFromFile` use only `FileShare.Read` and throw "being used by another process" on those. `CopyFile` is therefore a manual stream copy that then **re-stamps the destination's `LastWriteTimeUtc` from the source** (the sync engine compares write-times to decide copy/skip, so a naive copy's "now" timestamp would make every later run see the destination as newer); file *attributes* are intentionally not propagated. `CreateZipFromDirectory` builds the archive entry-by-entry and returns a **`ZipBuildResult`** (`Added` entry names for verbose logging + `Skipped` — `ZipSkippedFile(EntryName, Reason)` — for files that *still* couldn't be read): a single locked file is **skipped, not fatal**, so one bad file never aborts the whole archive. `ArchiveSyncProcessor` logs each skip as a **Warning** detail line and increments the run's **warning** count, so the handler's summary reports "completed with N warning(s)" while the archive is still produced from the readable files. (The ArchiveSync "only copy on change" fingerprint pass, `ComputeSourceFingerprint`/`TryHashFile`, likewise tolerates an unreadable file — it's omitted from the manifest rather than aborting the whole archive, leaving the zip pass to flag it as the warning, so a cloud-only file no longer fails the run.) A FolderPair/InstantSync copy that still fails on an unreadable file is caught by the existing crash-safe `CopyThroughTempAsync`, which classifies it via **`FileLock.IsSkippableReadError(ex, out reason)`** — a Win32 sharing/lock violation (HRESULT or the "being used by another process" message) **or** a cloud-file error (the `ERROR_CLOUD_FILE_*` family, Win32 358–405 / message contains "cloud file" — e.g. a OneDrive files-on-demand placeholder a session-0 service can't hydrate): either ⇒ logged as a **Warning** + counted as `BackupResult.Warnings` (the file is skipped this run, temp cleaned), with the `reason` in the line; any other copy failure ⇒ logged Error + counted as `Errors`. Either way the rest of the pair continues. **Include/exclude filtering** lives here too: `WildcardMatcher` (static, over the BCL `FileSystemName.MatchesSimpleExpression` — `*`/`?`, case-insensitive, no dependency) and `BackupFilter` (built from a parent's filter rows projected to `FilterRule(FilterDirection, FilterKind, string Pattern)`). **File/Folder matching is name-only** (matched in every sub-folder); **exclude-`Path` matching is by exact relative location**: `IncludesFile` (empty include list ⇒ all files, else the name must match an include), `ExcludesFolder` (a folder-name match skips the subtree), `ExcludesPath(relativeSegments)` (true when the joined relative path equals an exclude-path rule or sits beneath one — that file, or that folder and its subtree; normalised, case-insensitive), `IsFileInScope(name, ancestorFolderNames)` (now also applies `ExcludesPath` to `ancestors + name`) and `IsRelativePathInScope(relPath)` (splits a flat zip path). The folder-pair sync engine threads a `BackupFilter` through its recursion (filtering the copy loop, leaving out-of-scope target files/folders undeleted, and not recursing folders excluded by name *or* by path); the archive engine passes `IsRelativePathInScope` as `CreateZipFromDirectory`'s `includeEntry` predicate.
- `Logging/` — operation logging via a factory: `IOperationLogFactory`/`OperationLogFactory` (singleton, DB-factory) — `CreateAsync(name, level = OperationLogLevel.Info, profileId = null)` inserts a new `OperationLog` row (the `level` here is the **starting/floor** header level — it applies to a detail-less log and is then raised by the detail lines; `profileId` optionally ties the log to a profile so it cascade-deletes with it) and returns an `IOperationLogger` (impl `OperationLogger`) holding that row's id, an internal sequence counter, and the current header level. The logger appends `OperationLogDetail` rows via `AppendAsync(params string[] messages)` (Info) / `AppendAsync(OperationLogLevel level, params string[] messages)` / `ErrorAsync(message, exception?)` (Error) — **one row per message** (pass several in a single call to write several rows rather than one delimited string), all in one short-lived DbContext, auto-incrementing `Sequence` (`Interlocked`); **each detail row carries its own `Level`**, and `ErrorAsync` appends the exception's `Message` only (no stack trace) to the message. **The header `Level` is kept in step as the most severe line seen** — Debug/Info → Info, any Warning → Warning, any Error → Error; the header only rises (it touches the header row only when a line actually escalates it). The header message is **revised in place** via `SetSummaryAsync(message, level)` (updates `OperationLog.Name`, no extra record) — used to turn a "started…" title into a final "succeeded/failed in {duration}" summary so a run is a **single** log event (see `FolderPairHandler`); its `level` is a **floor** that can raise the header (e.g. force Error on a run that failed before logging an error line) but never lowers a level the lines already escalated to. The runtime logger type is `OperationLogger`, deliberately distinct from the `OperationLog` entity (and the enum is `OperationLogLevel`, not `LogLevel`, to avoid the `Microsoft.Extensions.Logging` clash). **`DeferredOperationLogger`** is an `IOperationLogger` wrapper that defers `IOperationLogFactory.CreateAsync` until the first line is actually written, so a unit of work that ends up doing nothing leaves **no** log row (`WasCreated` reports whether anything was written; `SetSummaryAsync` is a no-op until then). Used by the instant-sync watcher to suppress empty per-flush logs. The **read** side is `IOperationLogService`/`OperationLogService` (singleton, DB-factory): `GetPageAsync(page, size, filter?, includeMessages?, level?, profileId?)` → `PagedResult<OperationLog>` headers newest-first (`OrderByDescending(Id)` — SQLite can't `ORDER BY` a `DateTimeOffset`, each with its `Profile` `Include`d), optionally filtered by name (and detail message when `includeMessages`) via `EF.Functions.Like`, by exact `Level`, and by exact `ProfileId` (all AND-combined), and `GetDetailsAsync(logId)` → that log's `OperationLogDetail` lines ordered by `Sequence`. Consumed by the Logs section (`Components/Pages/BackupServicePage/Logs.razor`). **Push notifications:** `ILogWatcher`/`LogWatcher` (singleton) lets views refresh on a change rather than by polling — `OperationLogFactory`/`OperationLogger` call `logWatcher.Notify()` after **every** successful write (header create, detail append, summary rewrite; the watcher is an optional ctor dependency, so test-constructed factories/loggers without it are a no-op). `Notify()` is **coalesced/debounced** (a single timer, `DebounceMilliseconds`): a burst raises **at most one** `Changed` per window, and because the window is **not** re-armed by later writes while one is already pending, a long continuous run still fires `Changed` ~once per window instead of going silent until it stops. `Changed` is raised on a background thread and invokes each subscriber in isolation (a throwing handler can't break the others). The **Logs panel** subscribes in `OnAfterRender(firstRender)` and unsubscribes in `Dispose` (so selecting another sidebar panel — which removes the component — stops the pushes), refreshing the current page **in place** (preserving the user's expanded rows, filters and page) on each `Changed`. **Retention/purge:** `ILogRetentionService`/`LogRetentionService` (singleton; over the DB factory + `TimeProvider` + `ILogger`) owns the `LogRetentionSettings` row (`GetSettingsAsync` lazily seeds the 7/30 default; `UpdateSettingsAsync` clamps each to ≥ 1) and `PurgeIfDueAsync()` deletes log rows older than their retention. `PurgeIfDueAsync` is **guarded to run at most once per (UTC) day** via an in-memory `_lastPurgedUtcDate` + in-flight flag (thread-safe; marks done only on success so a failure retries), and is **fire-and-forget hooked after a log write** — `OperationLogFactory.CreateAsync` and `AuthenticationHistoryService.RecordAsync` both call `_ = logRetentionService?.PurgeIfDueAsync()` (an **optional** ctor dependency, so test-constructed instances skip it). Because **SQLite can't compare a `DateTimeOffset` column**, the purge projects `(Id, TimestampUtc)` into memory, finds the largest `Id` older than the cutoff (`Id` is monotonic with insertion time), and deletes by **`Id` range** (`ExecuteDeleteAsync`) — operation-log details first, then headers. `ClearOperationLogsAsync()` deletes **all** operation logs (details then headers, returns the header count) **and** all `BackupRun` rows immediately, regardless of retention — so it also resets the dashboard stats/charts. It backs the Logging panel's **Clear logs** button. The **Logging** settings section (`Components/Pages/Logging/LoggingPanel`) edits the two retention values and has a **Clear logs** button (a `btn-danger` guarded by a `ConfirmDialog`) that calls `ClearOperationLogsAsync` and toasts the number removed.
- `Profiles/` — `IProfileService`/`ProfileService` (singleton, DB-factory): `CreateAsync(name, type, scheduleCron, enabled, IReadOnlyList<FolderPairInput>)` inserts a `Profile` (with its `ProfileType` + `Enabled`) + its folder pairs (`FolderPairInput` is the service-layer DTO carrying `Name`, source/target, `AllowDeletions`, `IncludeSubFolders` and `OverwriteBehaviour`; `Id` 0 = new); `GetPageAsync(...)` reads a sortable page for the grid; `GetAsync(id)` loads one profile (with folder pairs) for editing; `GetSummariesAsync()` returns lightweight `ProfileSummary(Id, Name)` rows (name-ascending) for pickers/filters (e.g. the Logs profile filter); `UpdateAsync(id, …, enabled, IReadOnlyList<FolderPairInput>)` saves profile edits and syncs the folder pairs (updates matched ones by id, adds id-0 ones, removes the rest; the type is fixed and not updated); `SetEnabledAsync(id, enabled)` flips just the `Enabled` flag (used by the grid's inline toggle) and writes a profile-associated operation log named `Profile {name} was enabled/disabled` — a **self-describing** log with the message in the name and **no detail lines** (the convention for simple one-fact events); `DeleteAsync(id)` removes a profile (folder pairs and instant sync items cascade). **`ProfileService` owns only the generic profile fields** (name/type/schedule/enabled, its status seeding and the scheduler sync) — the **folder-pair data is split out into `IFolderPairService`/`FolderPairService`** (singleton, no DbContext: a stateless helper over the tracked `Profile.FolderPairs` graph, so a profile save and its folder-pair changes stay one unit of work and one operation log). `ProfileService` injects it and delegates: `Add(profile, inputs)` (build new pairs on create), `Sync(profile, inputs)` (update matched by id / add id-0 / remove the rest, **returning the human-readable change descriptions** the update log appends) and `DescribeForCreateLog(inputs)` (the per-pair detail lines for the create log). Since FolderPair is just one of several profile types, **add a type = its own equivalent data service** alongside `FolderPairService` rather than growing `ProfileService` — `IInstantSyncItemService`/`InstantSyncItemService` (over `Profile.InstantSyncItems`, DTO `InstantSyncInput`) and `IArchiveSyncItemService`/`ArchiveSyncItemService` (over `Profile.ArchiveSyncItems`, DTO `ArchiveSyncInput`; its `Sync` deliberately leaves `RunCount` untouched; it injects `ISecretProtector` to **encrypt the archive password** at rest — a blank password on edit keeps the stored one, turning protection off clears it — and never logs the password) are the worked second and third examples. `CreateAsync`/`UpdateAsync` take an `IReadOnlyList<FolderPairInput>` plus optional `IReadOnlyList<InstantSyncInput>` and `IReadOnlyList<ArchiveSyncInput>` lists, and a trailing optional `bool handleMissedSync` (persisted to `Profile.HandleMissedSync`; the scheduler does the catch-up — see `Scheduling/`); `ProfileService` `switch`es on the profile's `Type` and delegates to the matching data service (the other lists stay empty), and `GetAsync` `Include`s all three child collections (with `.ThenInclude(… .Filters)` for FolderPair/ArchiveSync). **Include/exclude filters ride on the FolderPair/ArchiveSync DTOs** as an optional trailing `IReadOnlyList<FilterInput>? Filters` (`FilterInput(Id, FilterDirection, FilterKind, Pattern)`; null = none, so existing call sites are unaffected); `FolderPairService`/`ArchiveSyncItemService` build the child `…Filter` rows on `Add` and reconcile them by id on `Sync` (same matched-update / id-0-add / drop-the-rest pattern as the pairs themselves), and emit filter lines into the create/update operation log. `BackupRunner` likewise `.ThenInclude`s the filters so the engines see them. The profile UI is `Components/Dialogs/ProfileDialog`, used for **both create and edit**: a null `ProfileId` creates, a set one loads + edits. When editing, the `Profile type` dropdown is **hidden** and the type description goes in the title (e.g. "Edit Folder Pairs Profile"); when creating, the dropdown (the `ProfileType` enum) is shown and selects which type-specific editor appears — `FolderPair` renders `FolderPairControl`, `InstantSync` renders `InstantSyncControl` (and disables the Schedule field), `ArchiveSync` renders `ArchiveSyncControl` (Schedule stays enabled — it's scheduled). The dialog also shows a **Handle missed sync** checkbox for scheduled types only (hidden for InstantSync, alongside the disabled Schedule field). `ArchiveSyncEditDialog` edits one archive (name, source/target + Browse, archive file name, Include Sub-Folders, a retention-mode `Dropdown` with help text, the keep count, and a Max-levels field shown only for GFS). The dialog also has an `Enabled` checkbox (defaults ticked). Add a profile type = one enum value + a matching control + a `switch` arm in the dialog. The fourth arm, `ProfileType.LightroomArchive`, renders `LightroomArchiveControl` (+ `LightroomArchiveEditDialog`, the InstantSync-shaped item editor — local source, connectable target) and, **only for this type**, three profile-level inputs above the item list (Lightroom folder + Browse, Raw formats, RAW folder name). Schedule/Handle-missed gating is by `IsWatcherDriven` (now `InstantSync` **or** `LightroomArchive`, replacing the old `IsInstantSync` check), since both are watcher-driven; the grid's `ShowStop` stays FolderPair/ArchiveSync-only, so LightroomArchive (like InstantSync) has Run-now/edit/delete but no Stop. Opened from the `Profiles` control, which lists profiles in a sortable, paged grid; an **Enabled** column hosts a `ProfileEnabledControl` (toggle persists inline via `SetEnabledAsync`), and the last column has a `play.png` icon button (**Run now**), a `profile-settings.png` icon button (edit) and a `remove.png` icon button (delete, via `ConfirmDialog`). **Run now** fires `IBackupRunner.RunAsync(id, manual: true)` on a background `Task.Run` (so a long backup doesn't block the circuit; the status-change events refresh the grid as it progresses, and `RunAsync` records its own failures so fire-and-forget is safe) — the run goes through the normal runner path, just logged with a `[Manual]` prefix. The **Status** column reads the live status from `IProfileStatusService` (see below) and is **not** sortable (status isn't a DB column). The run, edit and delete icon buttons are **disabled while that profile is `Running`** (`disabled="@IsRunning(id)"`, re-evaluated on each status-change re-render; `.icon-btn:disabled` greys them) — so a profile can't be run twice from the UI (the runner's `TryBeginRun` is the real guard, but the disabled button avoids the obvious double-click). **While a scheduled type (FolderPair/ArchiveSync) is running, those three disabled buttons are replaced by a single `stop.png` Stop button** (`ShowStop(profile)` = running && type is FolderPair/ArchiveSync) that calls **`IBackupRunner.RequestStop(id)`** to safely cancel the in-progress run: the runner keeps a per-profile `CancellationTokenSource` (a linked source created in `RunAsync`, so the host shutdown token *or* the Stop button can cancel it) and `RequestStop` cancels it. The run **unwinds cleanly leaving no temp files** — `FolderPairSynchronizer` threads the token into the crash-safe copy (`CopyToAsync(…, ct)`) and its `catch (OperationCanceledException)` deletes the partial `.tmp` before rethrowing, so a Stop mid-large-file is prompt and clean. The handler treats cancellation as a **Warning, not an Error**: its `catch (OperationCanceledException)` appends a warning line and rewrites the summary to `"… was cancelled after {duration} — …"` at Warning level (outcome `CompletedWithWarnings`, so the dashboard shows it amber), and the runner's `catch (OperationCanceledException) when (cts.IsCancellationRequested)` returns the profile to **Idle** (not Error) so it simply **waits for its next scheduled run**. (InstantSync has no Stop button — it's watcher-driven with no scheduled run.) Opening the edit dialog or the delete-confirmation **locks** the profile via `IProfileStatusService.Lock` (unlocked on cancel/save and in `Dispose`) so a scheduled run won't fire mid-edit — the runner skips a locked profile. `ProfileService` keeps the status tracker in step with the DB: `CreateAsync` seeds the new profile to `Idle`, `DeleteAsync` removes it (which also clears any lock).
  - **`IProfileStatusService`/`ProfileStatusService`** (singleton) — the **in-memory** run-status tracker for profiles (`ProfileStatus` Idle/Running/Error), backed by a `ConcurrentDictionary` (mutated by the background runner, read by the UI). `Get(id)` (defaults to `Idle` for unknown ids), `Set(id, status)` (raises the `event Action<int>? Changed` with the profile id), `Remove(id)`. It also tracks a per-profile **progress percent**: `SetProgress(id, percent)` (clamps 0–100, raises a **separate** `event Action<int>? ProgressChanged` only when the integer value changes — so a run pushes ~100 cheap updates, not a flood) and `GetProgress(id)` (null when not running/known); `Set` to a non-Running status clears the percent. The grid shows **"Running - {percent}%"** in the Status cell and subscribes to `ProgressChanged` to re-render **just the cell** (`InvokeAsync(StateHasChanged)`, no DB reload) — distinct from `Changed`, which reloads the page on a status transition. The percent is **file-count based** for FolderPair/InstantSync runs: each handler pre-counts work via `IFolderPairSynchronizer.CountFilesAsync` (a cheap source-only walk applying the same include/exclude + IncludeSubFolders scoping) and threads a `ProfileProgressReporter` (`IProgress<int>`) into `SyncAsync`, which reports `1` per in-scope file handled; ArchiveSync reports **per-item, sub-divided**: each item is an equal slice of the run, and within a slice the processor reports a 0..1 completion fraction where the first **75%** tracks files added to the zip (via a per-entry `onEntryProcessed` callback on `IBackupFileSystem.CreateZipFromDirectory`, against a pre-count of in-scope files) and the final **25%** tracks bytes copied to the target (a manual buffered copy in `CopyThroughTempAsync`); the handler maps `(itemIndex + fraction) × 100 / totalItems` to the percent via a synchronous `IProgress<double>`. Progress is never persisted (cleared when the run leaves Running). It also tracks a per-profile **lock** (`Lock`/`Unlock`/`IsLocked`, a separate set) used to mark a profile that's open in an edit/delete dialog — `BackupRunner` skips a locked profile so a scheduled run can't fire mid-edit; `Remove` clears the lock too. The status is also the **single-instance gate**: `IsRunning(id)` and the atomic `TryBeginRun(id)` (sets Running iff not already Running, returns whether it began) enforce **one run per profile at a time** — the scheduler pre-checks `IsRunning` so it won't even spawn a thread, and `BackupRunner` calls `TryBeginRun` as the authority. Status is **never persisted** — on startup `Program.cs` seeds every existing profile to `Idle`. The `Profiles` grid (`@rendermode InteractiveServer`) subscribes to `Changed` in `OnInitialized` and, when the changed profile is on the current page, **reloads the page data** (`InvokeAsync(async () => { await LoadAsync(_page); StateHasChanged(); })`) so the live Status cell *and* the entity-sourced columns it doesn't track (notably `DateLastRun`, which the runner writes in the DB) both refresh as a backup runs (`IDisposable` unsubscribes). The transitions are driven by the scheduler: `BackupRunner` sets `Running` then `Idle`, and `FolderPairHandler`'s catch-all sets `Error` (see `Scheduling/`).
- `Dashboard/` — `IDashboardService`/`DashboardService` (singleton, DB factory) aggregates `BackupRun` history into a `DashboardData` view model for the Dashboard section. `GetAsync(days)` pulls the recent runs newest-first by `Id` (SQLite can't `ORDER BY`/compare a `DateTimeOffset`, so it filters/groups by date **in memory** — the same approach as `LogRetentionService`) and computes: stat cards (profiles total/enabled, runs in period, success-rate %, files synced, last-run time), `OutcomesByDay` (Success/**CompletedWithWarnings**/CompletedWithErrors/Failed per day — the stacked-column chart, drawn green/**amber**/red where the amber series is warnings and red sums errors+failed), `BytesByDay` (total **bytes copied** per day — a bar chart whose y-axis auto-scales the unit B/KB/MB/GB via a JS label formatter), `DurationByProfile` (each profile's avg run time split into success-, **warning-** and failure-weighted portions — green/amber/red, the amber part sized by the warning rate and red by the error rate), and `RecentRuns` (the table, with a `Warnings` column). **Scheduled-task runs appear here too:** the `BackupRun` projection reads `RunKind Kind` and names a row from the profile *or* the scheduled task (`r.ScheduledTask.Name`), `RecentRun` carries `Kind`, and the Recent-runs table renders the type cell as **"Scheduled Task"** when `Kind == ScheduledTask` (else the `ProfileType` description). Task rows have zero file/byte counts, so they contribute to run/outcome/success-rate/duration stats but not to Files/Bytes/Archives totals. **Counts split:** `FilesSyncedInPeriod` is regular files (FolderPair + InstantSync `Copied+Updated`); ArchiveSync zips are counted separately as `ArchivesCreatedInPeriod` (shown as a sub-line under the Files-synced stat). The "Data copied" bar chart sits to the **left** of "Run time by profile" (the old full-width run-time panel is split in half). The outcome pill maps Success→green, **CompletedWithWarnings→amber**, CompletedWithErrors/Failed→red (`log-level-info`/`-warning`/`-error`). The view-model DTOs live in `Dashboard/DashboardData.cs`. **Charts use `Blazor-ApexCharts`** (NuGet `Blazor-ApexCharts`, namespace `ApexCharts`): registered via `builder.Services.AddApexCharts()` in `Program.cs`, imported in `Components/_Imports.razor` (`@using ApexCharts`), and the runtime script `_content/Blazor-ApexCharts/js/blazor-apexcharts.js` is referenced in `App.razor` after `blazor.web.js`. The Dashboard component (`Components/Pages/BackupServicePage/Dashboard.razor(.cs)`) follows the `Logs.razor` live-refresh pattern (subscribe `ILogWatcher.Changed` for run completions + `IProfileStatusService.Changed` for the live "running now" count in `OnAfterRender`; `InvokeAsync`+guard; `Dispose` unsubscribes), with a 7/14/30-day period `Dropdown`. Chart colours are literal hex matching the theme (JS options can't read CSS vars); after a data refresh it calls each chart's `UpdateSeriesAsync`.
- `Connections/` — **remote-resource connections** (an extensible `ConnectionType`; **SMB**, **Google Drive** and **USB** so far). A `Connection` (table) has `Name`, `ConnectionType Type`, `DateCreated` and a **1:1 child** `SmbConnectionSettings` (`Database/`, keyed by `ConnectionId`, cascade-deleted; `Host`/`Port`/`ShareName`/`Domain?`/`Username`/`PasswordEncrypted`/`RootFolder`). **The SMB password is encrypted at rest** (not hashed — SMB needs the plaintext to authenticate): `Security/ISecretProtector`/`DataProtectionSecretProtector` wraps **ASP.NET Core Data Protection** (`IDataProtectionProvider`, part of the Web SDK shared framework — no extra package; **cross-platform**, Windows + Linux). `Program.cs` registers `AddDataProtection().PersistKeysToFileSystem({DataDir}/keys).SetApplicationName("BackupService")` so the key ring lives next to the DB (`BackupDatabaseLocation.GetDataDirectory()`) and protected secrets stay decryptable across restarts. On Windows the key ring is itself DPAPI-protected; the encrypted passwords are bound to that key ring, so passwords must be re-entered if the keys are lost (don't add `[SupportedOSPlatform("windows")]` here — the goal is cross-platform). **Legacy migration:** an earlier version encrypted passwords with raw Windows DPAPI; `Connections/ConnectionSecretMigrator` (run from `Program.cs` at startup, after the EF migrations) transparently re-encrypts any old-format password into Data Protection — it skips values the current protector can already read, and for the rest decrypts via `Security/ILegacySecretReader`/`DpapiLegacySecretReader` (the only remaining DPAPI use, Windows-guarded; the `ProtectedData` package is kept solely for this) and re-`Protect`s. Idempotent; an unreadable value is logged for re-entry, not touched. `IConnectionService`/`ConnectionService` (singleton, DB factory + `IOperationLogFactory` + `ISecretProtector`) is the CRUD (mirrors `IProfileService`: `CreateAsync`/`UpdateAsync`/`GetPageAsync`/`GetAsync`/`GetSummariesAsync`/`DeleteAsync`, a profile-less op-log per mutation, type fixed after create, **never logs the password**); a blank password on **edit** keeps the stored one, and `DeleteAsync` returns a `ConnectionDeleteResult` that's **blocked** when a `FolderPair` references the connection. `IConnectionResolver`/`ConnectionResolver` loads a connection and **decrypts** it into a runtime `SmbConnectionInfo` (kept off the grid read path; used by Test, the remote browser and the sync engine). `Connections/Smb/ISmbConnector`/`SmbConnector` talks to the server via the **SMBLibrary** NuGet (`SMB2Client.Connect`/`Login`/`TreeConnect` → `ISMBFileStore`): `TestAsync` (a friendly `ConnectionTestResult`), `ListDirectoriesAsync` (for the picker) and `CreateDirectoryAsync` (the picker's New-folder button). UI: the home **Connections** section (`Components/Pages/BackupServicePage/ConnectionsControl`) is a Profiles-style grid (with a **Free space** column — see below); `Components/Dialogs/ConnectionDialog` (create/edit, type-`Dropdown` hidden on edit) hosts the type editor `Components/Controls/SmbConnectionEditor` (fields + **Test** button + remote **Browse**), and `Components/Dialogs/SmbFolderBrowserDialog` is a simple themed picker over `ISmbConnector.ListDirectoriesAsync` with an optional **`AllowCreateFolder`** New-folder button (target side only — `ConnectionLocationField` passes its `AllowCreateFolder` through) that calls `ISmbConnector.CreateDirectoryAsync`. A **`ConfineToRoot`** flag controls its rooting: the **connection editor** browses the whole share and returns a share-root-relative path (that becomes the connection's `RootFolder`); the **profile editors** pass `ConfineToRoot=true`, so browsing is confined to the connection's `RootFolder` subtree (Up stops there, you can't navigate above it) and the chosen path is returned **relative to `RootFolder`** — which is exactly what the engine expects (`EndpointFileSystemFactory` rebuilds the share path as `RootFolder + storedPath`). **Connections are usable as a source/target on all profile types — but the connection is **profile-level**, chosen once for the whole profile (`Profile.SourceConnectionId`/`Profile.TargetConnectionId`, each null = local), **not** per row. Each `FolderPair`/`InstantSyncItem`/`ArchiveSyncItem`/`LightroomArchiveItem` row carries only its `SourceFolder`/`TargetFolder` (relative to the profile's connection root); the engines (`FolderPairSynchronizer.SyncAsync`/`CountFilesAsync`, `ArchiveSyncProcessor.CreateArchiveAsync`, `LightroomArchiveProcessor.ProcessBatchAsync`, and the InstantSync/Lightroom watchers) take the connection ids from the profile and pass them to `IEndpointFileSystemFactory.ResolveAsync`. `ProfileDialog` renders the connection as two `Components/Controls/ConnectionLocationField ShowFolder="false"` dropdowns **above the Schedule** — a Source connection (FolderPair/ArchiveSync only; the watcher types have a local-only source) and a Target connection (all types, USB excluded) — and the per-row editors use `ConnectionLocationField ShowLocation="false" ConnectionId="@…"` (folder + connection-aware Browse against the profile's connection, no per-row dropdown). The single-row migration `MoveConnectionsToProfile` **promotes** each profile's connection from its first (lowest-Id) child row and **deletes** any row whose connection differed. **Per-type wiring:** FolderPair — both sides connectable. ArchiveSync — `ArchiveSyncProcessor` resolves both endpoints: a remote **source** is staged to a local temp folder before zipping (`CreateZipFromDirectory` is local-only), a remote **target** receives the finished zip cross-fs (and retention runs against the target filesystem). InstantSync/LightroomArchive — the **source is local-only** (no `FileSystemWatcher` over an SMB session, so `ProfileDialog` shows no Source connection for these and the watcher gates the whole profile defensively). An InstantSync profile with a remote **target** is flushed by a full reconcile through the synchronizer instead of the local incremental `InstantSyncProcessor` (local→local keeps the fast incremental path). **`IConnectionService` carries a `SmbConnectionInput` and a `GoogleDriveConnectionInput` overload of `CreateAsync`/`UpdateAsync` (type-specific, lowest blast radius); `IConnectionResolver` adds `GetGoogleDriveInfoAsync` (decrypts) and `GetTypeAsync` (so `EndpointFileSystemFactory` and `ConnectionLocationField` branch by type); `EndpointFileSystemFactory.ResolveAsync` `switch`es on `GetTypeAsync` to build either an `SmbBackupFileSystem` or a `GoogleDriveBackupFileSystem`.** **The grid's "Free space" column** is filled by `IConnectionSpaceService`/`ConnectionSpaceService` (singleton): it resolves a connection id by type and asks the matching connector for a `StorageSpace(TotalBytes, FreeBytes, bool Unlimited)` — `ISmbConnector.GetFreeSpaceAsync` (SMBLibrary `GetFileSystemInformation` → `FileFsFullSizeInformation` allocation-unit math) and `IGoogleDriveConnector.GetFreeSpaceAsync` (Drive `About.StorageQuota` `Limit`/`Usage`; null `Limit` ⇒ `Unlimited`). Every method is **best-effort** — a `null` `StorageSpace` (unreachable/error) renders as "—". `ConnectionsControl` loads each row's space on a **background task** (each is a live round-trip) and fills it in via `StateHasChanged`, with a `_generation` guard against superseded page/sort loads and a `CancellationTokenSource` cancelled on reload/`Dispose`; values are humanised by **`Extensions/ByteSize.Humanize`** (the C# counterpart of the dashboard's JS byte formatter, base-1024 B/KB/MB/GB/TB/PB, invariant culture).
  - **Google Drive (the second connection type) authenticates via OAuth 2.0, not username/password.** Its 1:1 child is **`GoogleDriveConnectionSettings`** (`Database/`; a `bool UsesBuiltInClient`, `ClientId`, `ClientSecretEncrypted`, `RefreshTokenEncrypted` — the secret + token encrypted at rest via the same `ISecretProtector`, **never logged** — `AccountEmail?` for display, and a `RootFolder?` **name-path** relative to My Drive root, like SMB's). **Two ways to supply the OAuth client:** by default the app's **built-in** client is used (one-click Authorize; its id/secret come from config — `GoogleDrive:ClientId`/`GoogleDrive:ClientSecret`, set via user-secrets or an environment overlay, **never committed** — surfaced as a singleton `Connections/GoogleDrive/GoogleDriveAppCredentials`), and the editor's **Advanced** toggle lets a user supply their **own** client instead (stored on the connection). A connection records `UsesBuiltInClient` because the refresh token is tied to whichever client issued it: a built-in connection stores no id/secret and `ConnectionResolver` fills them from `GoogleDriveAppCredentials` at run time; a custom one stores (encrypted) its own. When the built-in client isn't configured the editor falls back to Advanced-only. Either client must register our callback as a redirect URI — `http://localhost:5080/connections/google/callback` (dev) and `http://localhost:55000/connections/google/callback` (prod) — and have the OAuth consent screen **published to Production** (not "Testing", where refresh tokens expire after 7 days). The OAuth client must be type **Web application** with the Drive API enabled. **In-app consent flow:** `Connections/GoogleDrive/IGoogleOAuthFlowService`/`GoogleOAuthFlowService` (singleton) holds pending attempts keyed by an unguessable `state`, each a `TaskCompletionSource`; `Begin(clientId, clientSecret, redirectUri)` (or `BeginBuiltIn(redirectUri)`, which uses the injected built-in client so the editor never handles the built-in secret) builds the consent URL (`scope=drive`, `access_type=offline`, `prompt=consent` so a refresh token is always issued), the editor opens it in a new tab and `await`s `WaitAsync(state, timeout)`, and the **`/connections/google/callback`** minimal endpoint in `Program.cs` (anonymous; resolves a flow by `state`) calls `CompleteAsync(state, code, error)` which exchanges the code (Google.Apis.Auth) and resolves the waiter with the **refresh token + account email**. The refresh token lives only in memory until the connection is saved (then encrypted). `Connections/GoogleDrive/IGoogleDriveConnector`/`GoogleDriveConnector` is the Google counterpart of `ISmbConnector` (`TestAsync`/`ListDirectoriesAsync`/`CreateDirectoryAsync`, name-paths resolved to folder ids per call). **`FileSystem/GoogleDrive/GoogleDriveBackupFileSystem`** implements the full `IBackupFileSystem` over `Google.Apis.Drive.v3` (NuGet `Google.Apis.Drive.v3`, cross-platform — **no `[SupportedOSPlatform]`**): Drive is id-based but the engine uses backslash **name-paths**, so it resolves each path → folder/file id (walking from My Drive root, cached per session) and exposes Drive as a tree; `OpenRead` downloads to a self-deleting local temp, `OpenWrite` buffers to a local temp and uploads on close, `GetTempFilePath`/`CreateZipFromDirectory` throw `NotSupportedException` (local-only, exactly like SMB), and `GetZipComment` ranged-reads the EOCD tail. **Drive's `modifiedTime` is only millisecond-precise** (which would make the engine's exact write-time compare re-copy every run), so `SetLastWriteTimeUtc` also stores the source's exact tick count in a private `appProperties` entry that `GetLastWriteTimeUtc` reads back — a round-tripped timestamp is exact. UI: `Components/Controls/GoogleDriveConnectionEditor` (one-click **Authorize with Google** + status line by default, an **Advanced** "use my own OAuth client" toggle revealing Client ID/secret, Root folder + Browse, Test) and `Components/Dialogs/GoogleDriveFolderBrowserDialog` (mirrors `SmbFolderBrowserDialog` over `IGoogleDriveConnector`); `ConnectionDialog` switches the editor on `ConnectionType`, and `ConnectionLocationField` branches the Browse picker on `GetTypeAsync`. **Add a connection type = enum value + settings entity (+migration) + DTOs + connector + `IBackupFileSystem` impl + service/resolver/factory arms + editor/browser + `ConnectionDialog` arm**, mirroring SMB or Google Drive.
  - **USB (the third connection type) is bound to a specific physical device and is only valid while it's plugged in.** It covers **two device kinds** (`Enumerations/UsbDeviceKind`: `MassStorage`/`Mtp`) under one type — its 1:1 child **`UsbConnectionSettings`** (`Database/`) carries `Kind`, the mass-storage identity (`HardwareSerial?` preferred + `VolumeSerial` fallback), the MTP identity (`MtpSerial?` — the WPD `DeviceId`), `DeviceLabel?` (display) and `RootFolder?` (relative to the device root for mass-storage; device-absolute for MTP). **No secret** (no `ISecretProtector` for USB). The device picker in the editor lists **both** connected drives and MTP/PTP portable devices; you pick whichever is your device. Because drive letters are reused (Camera A and B can both mount `D:`), the connector pins to the device identity, not the letter: at registration the user picks a connected removable drive, and on each connect the arriving drive's identity is matched via **`Connections/Usb/UsbDeviceMatcher`** (pure, unit-tested — *prefer hardware*: both serials present ⇒ hardware must match, else compare volume serials). Device identity is read by **`IUsbDeviceInspector`/`WindowsUsbDeviceInspector`** (Windows-only P/Invoke — `GetVolumeInformation` for the volume serial, `DeviceIoControl(IOCTL_STORAGE_QUERY_PROPERTY)` for the hardware serial; **no NuGet package**; `NullUsbDeviceInspector` off Windows). **`IUsbConnector`/`UsbConnector`** (cross-platform over the inspector + `DriveInfo`) does `EnumerateConnectedDevices` (the editor's picker), `FindMountPath` (the current drive root of the matching device, or null), `TestAsync` and `GetFreeSpaceAsync` (the grid column; "—" when unplugged). **`EndpointFileSystemFactory` resolves a USB connection by kind:** a **mass-storage** connection → the LOCAL `BackupFileSystem` rooted at the device's current drive letter + `RootFolder` (no new impl); an **MTP** connection → **`FileSystem/Mtp/MtpBackupFileSystem`** (Windows-only, **read-only**, over the **`MediaDevices`** NuGet — MIT, no deps; modeled on `GoogleDriveBackupFileSystem`: device-absolute backslash paths, `OpenRead` downloads to a self-deleting temp, every write/archive member throws since USB is source-only). Either **throws when the device isn't connected** (runs only fire on connect, so that's the defensive path). **USB is source-only:** `ConnectionLocationField` has an `AllowUsb` flag (default true) that the **target-side** fields set false; a USB source browses by kind — a mass-storage source via the **local** `FolderBrowserDialog` (rooted at the mount path), an MTP source via **`Components/Dialogs/MtpFolderBrowserDialog`** (over `IMtpDeviceInspector.ListDirectories`, confined to the connection root, returning a relative path). UI: `Components/Controls/UsbConnectionEditor` (combined drive+MTP device picker + Refresh + connected/disconnected status + Root folder/Browse). MTP devices add **`IMtpDeviceInspector`/`WindowsMtpDeviceInspector`** (Windows-only, over MediaDevices: `EnumerateMtpDevices`/`IsConnected`/`ListDirectories`/`GetFreeSpace`; `NullMtpDeviceInspector` off Windows) and a second matcher `UsbDeviceMatcher.MatchesMtp` (serial equality, unit-tested). `UsbConnector` + `ConnectionSpaceService` branch by `Kind`. **Run-on-connect:** `Scheduling/Usb/UsbDeviceWatcherService` (Windows-only hosted service; a hidden top-level window on a dedicated message-loop thread, mirroring `WindowsTrayService`) handles **both** kinds: `WM_DEVICECHANGE` **volume** broadcasts (`DBT_DEVTYP_VOLUME` → `UsbDriveLetters.FromUnitMask`) for mass-storage, and **`DBT_DEVNODES_CHANGED`** (debounced) → re-enumerate MTP serials and **diff** against a startup-seeded snapshot for portable devices. Either path matches the registered USB connections (mass-storage by drive identity, MTP by serial), **logs** + **notifies** (`IDesktopNotifier`), records the match (so a later removal can be logged), and runs every **enabled FolderPair/ArchiveSync** profile whose source is a matched connection via `IBackupRunner.RunAsync(id, manual:false)` (shared `HandleMatchedAsync`); on disconnect it logs + notifies. It is **not** wired through `ProfileService` (it resolves matching profiles from the DB at event time). In `ProfileDialog`, a FolderPair/ArchiveSync profile whose profile-level **source connection** is USB (`IsDeviceTriggered`, computed from `Profile.SourceConnectionId`'s type) **hides the Schedule field**, shows the note *"Automatically runs when the device is plugged in."* and is saved with a **null cron** (so `BackupSchedulerService` never schedules it).
- `ScheduledTasks/` — the **Scheduled Tasks** feature: cron-scheduled tasks that run one or more **ordered OS-command steps** in sequence (a parallel stack to backup profiles — it runs processes, not file copies — reusing the same schedule machinery). `IScheduledTaskService`/`ScheduledTaskService` (singleton, DB factory + `IOperationLogFactory` + the step/status/scheduler services) is the CRUD, mirroring `IProfileService`: `CreateAsync`/`UpdateAsync`/`GetPageAsync(page,size,sort,desc,filter?,enabled?)`/`GetAsync`/`SetEnabledAsync`/`DeleteAsync`, a profile-less op-log per mutation, seeds status `Idle` on create / `Remove` on delete, and calls `scheduler.SyncAsync(id)` after every mutation. The step data is split out into `IScheduledTaskStepService`/`ScheduledTaskStepService` (stateless over `ScheduledTask.Steps`, DTO `ScheduledTaskStepInput(Id, Order, Name?, Command?, Arguments?, WorkingDirectory?, RunViaShell, Kind = Command, Script?)` — same matched-by-id / id-0-add / drop-the-rest `Add`/`Sync`/`DescribeForCreateLog` shape as `ArchiveSyncItemService`). **`IScheduledTaskStatusService`/`ScheduledTaskStatusService`** is the in-memory run-status tracker (`ProfileStatus` Idle/Running/Error per task id) — a trimmed copy of `ProfileStatusService` (Get/Set/IsRunning/`TryBeginRun`/Lock/Unlock/IsLocked/Remove/`Changed`, no progress percent), a **separate instance** so task ids and profile ids don't collide. `Program.cs` seeds every existing task to Idle on startup. The scheduler/runner/process-runner live in **`Scheduling/ScheduledTasks/`** (see that bullet). The UI is the home **Scheduled Tasks** section: `Components/Pages/BackupServicePage/ScheduledTasksControl` (a Profiles-style sortable/paged grid — Name, Schedule (humanised via `ScheduleDefinition.Describe`), Steps count, inline Enabled toggle via `ProfileEnabledControl`+`SetEnabledAsync`, live Status, Last run, Next run, and per-row Run-now/Stop/edit/delete), `Components/Dialogs/ScheduledTaskDialog` (create/edit — Name/Description/Enabled + the shared `ScheduleDialog` + Handle-missed-sync + the steps editor), `Components/Controls/ScheduledTaskStepsControl` (+ `ScheduledTaskStepModel`; the ordered step list with up/down reorder, opening `Components/Dialogs/ScheduledTaskStepEditDialog` — one step: name, a **Type** `Dropdown` (Command/PowerShell); for **Command** a Run-via-shell checkbox flipping the Command/Executable label + Arguments (direct mode only); for **PowerShell** a 6-line script textarea; plus an optional Working directory + Browse via `ConnectionLocationField LocalOnly`). Add behaviour mirrors profiles. Unit-tested via `ScheduledTaskStepServiceTests` (the sync logic) and `ScheduledTaskRunnerTests` (the runner, below).
- `Scheduling/ScheduledTasks/` — the scheduled-task **scheduler, runner and process seam**. **`IScheduledTaskScheduler`/`ScheduledTaskSchedulerService`** mirrors `BackupSchedulerService` (BackgroundService in the same **three-role** registration: singleton, `IScheduledTaskScheduler`, hosted service; in-memory entries, `SemaphoreSlim` wake, `SyncAsync(taskId)`, persists `ScheduledTask.DateNextRun` via `ExecuteUpdateAsync`, startup load + missed-run catch-up) — **reusing** the public statics `BackupSchedulerService.GetNextOccurrence`/`ShouldCatchUp` rather than duplicating cron logic. It fires due tasks via `IScheduledTaskRunner.RunAsync(taskId, manual:false)`, skipping ones already running. **`IScheduledTaskRunner`/`ScheduledTaskRunner`** is `BackupRunner` + a handler in one (only one kind of work): loads the task with `Steps` ordered, skips if `IsLocked`, `TryBeginRun` (Running), creates **one** `OperationLog` (`[Manual] ` prefix for Run-now), runs each step via `IProcessRunner`, appends an **Info** line per stdout line and an **Error** line per stderr line (prefixed with the step label), **stops at the first non-zero exit** and marks the run **Failed**, supports `RequestStop(taskId)` (a linked `CancellationTokenSource` per task id — kills the running process tree; cancellation is a **Warning**, status → Idle), and in a `finally` stamps `DateLastRun`, records one `BackupRun` (`Kind=ScheduledTask`, zero counts) via `IBackupRunRecorder.RecordScheduledTaskAsync`, and rewrites the log summary (ran successfully / failed at {step} / was cancelled). **`IProcessRunner`/`ProcessRunner`** is the unit-testable execution seam (like `BackupFileSystem`, not itself unit-tested): runs one step via `System.Diagnostics.Process` (`RedirectStandardOutput`/`Error`, `UseShellExecute=false`, `CreateNoWindow`, optional `WorkingDirectory`). A **Command** step runs the executable directly, or — when `RunViaShell` — wrapped in `cmd.exe /c …` on Windows or `/bin/sh -c …` elsewhere (`OperatingSystem.IsWindows()`, cross-platform, no `[SupportedOSPlatform]`). A **PowerShell** step writes its `Script` to a temporary `.ps1` (UTF-8 BOM) and runs it via `pwsh -NoProfile -NonInteractive -ExecutionPolicy Bypass -File <temp>` when `pwsh` is on `PATH`, else Windows `powershell` (the temp file is deleted in a `finally`). Either way it streams stdout/stderr to completion, honours the token (kills the process tree on cancel), and returns `ProcessRunResult(ExitCode, StandardOutput, StandardError)`. The runner is unit-tested against a fake `IProcessRunner` in `ScheduledTaskRunnerTests` (order, stop-on-failure, stdout/stderr capture, BackupRun recording, `[Manual]` prefix, cancellation → warning/idle).
- `Authentication/` — `IAdminCredentialService` / `AdminCredentialService`: seeds the single admin row, verifies login credentials, and changes the password (`ChangePasswordAsync` verifies the current password, then re-hashes), using `PasswordHasher<AdminCredential>` (PBKDF2). Registered as a singleton; uses the DB factory. The settings UI for this lives in `Components/Pages/Authentication/` (`AuthenticationPanel`); its `ChangePasswordDialog` modal lives in `Components/Dialogs/`.
- `IAuthenticationHistoryService` / `AuthenticationHistoryService` (singleton) — writes an `AuthenticationHistory` audit row (`EventType` + UTC timestamp) for each login success/failure (recorded in `Login.razor.cs`) and password change (recorded in `ChangePasswordDialog`), and reads it back paged (`GetPageAsync`, newest first). `AuthenticationPanel` shows this as a paged table below the Change Password button.
- `Options/` + `Notifications/` + `Hosting/IStartupManager` — the **Settings → Options** desktop-integration toggles. The single-row **`AppOptions`** table (`Database/`, like `LogRetentionSettings`; `StartWithWindows`/`ShowTrayIcon`/`AllowNotifications`, all default **false**) is owned by **`Options/IAppOptionsService`/`AppOptionsService`** (singleton, DB factory; lazy-seed + `UpdateSettingsAsync` + a `Changed` event). `OptionsPanel` (`Components/Pages/Options/`) edits the three via themed `ToggleButton`s; **Save** persists, calls `IStartupManager.Apply(startWithWindows)`, and toasts. These are **Windows-only** behaviours; off Windows the settings persist but no-op. **Autostart** — `Hosting/IStartupManager` (`WindowsStartupManager` writes/removes the per-user `HKCU\…\Run\BackupService` value = `"{exe}" -background`, using `Microsoft.Win32.Registry` which the net10.0 framework already provides — no package; `NoopStartupManager` off Windows). `Program.cs` reconciles the Run-key with the saved option at startup so the registered path tracks the exe after a redeploy. **Tray icon + notifications** — `Notifications/WindowsTrayService` (Windows-only, registered in the three-role pattern: singleton + `IDesktopNotifier` + hosted service) runs a Win32 message loop on a dedicated STA thread with a hidden top-level window and a `Shell_NotifyIcon` tray icon (double-click opens the admin UI; a right-click menu offers Open / Exit via `IHostApplicationLifetime`). The icon is present when `ShowTrayIcon` **or** `AllowNotifications` is on (a balloon attaches to a tray icon); it caches the options and refreshes via `IAppOptionsService.Changed`. **`IDesktopNotifier`** (`NotifyBackupCompleted`/`NotifyTaskCompleted` + `NotifyDeviceConnected`/`NotifyDeviceDisconnected`; `NullDesktopNotifier` off Windows) shows a balloon (`NIF_INFO`, severity-coloured by `RunOutcome` for runs) — for a **discrete** run completion (`NotifyBackupCompleted`) and **start** (`NotifyBackupStarted`) it's hooked as an **optional** trailing `IDesktopNotifier? notifier = null` ctor param on the four backup handlers (`FolderPairHandler`/`ArchiveSyncHandler`/`InstantSyncHandler`/`LightroomArchiveHandler` — start at the top of `HandleAsync`, completion in the `finally` after the run summary, skipped when cancelled) and `ScheduledTaskRunner`. **Each backup run also honours the profile's own notification settings** (`Profile.NotificationsEnabled`/`NotifyOnStart`/`NotifyOnComplete`): a handler fires the start balloon only when `NotificationsEnabled && NotifyOnStart`, and the completion balloon only when `NotificationsEnabled && NotifyOnComplete` (all still under the global `AllowNotifications` gate); the continuous InstantSync/Lightroom **watcher flushes are deliberately not hooked** (too noisy); the device methods are called by `UsbDeviceWatcherService` on connect/disconnect. All balloons are gated by the `AllowNotifications` option. **On-screen progress windows** are a separate Windows-only feature: `Notifications/WindowsProgressWindowService` (registered as a hosted service in the Windows block) runs its own message-loop thread (like `WindowsTrayService`) and, for any profile with `Profile.ShowProgressWindow`, draws a small borderless always-on-top GDI window in the bottom-right showing the profile name, action and a progress bar; multiple running profiles **stack vertically**. It's driven entirely by `IProfileStatusService` (`Changed` → show/hide as a profile enters/leaves Running, looking up the profile's name/type/flag from the DB; `ProgressChanged` → repaint the bar) and is independent of the `AllowNotifications` balloon gate. **Future (not built):** the autostart could be surfaced per-machine vs per-user.
- **Auth model:** cookie authentication for a **single admin account** (username `admin`, default password `admin`, stored **hashed** in the `AdminCredentials` table — a future change will allow editing it). Session uses a **30-minute sliding (inactivity) expiry**. Every page requires authentication via `@attribute [Authorize]` in `Components/Pages/_Imports.razor` (applies to all pages there); the login page opts out with `@attribute [AllowAnonymous]`. Enforcement is by `AuthorizeRouteView` in `Routes.razor`, **not** a global authorization `FallbackPolicy` — a fallback policy is enforced at the Blazor endpoint level and would also block `/login`, causing a redirect loop. `Login.razor` is a **static SSR** page (no interactive render mode) so its form POST is a real HTTP request that can write the auth cookie via the cascaded `HttpContext`.
- Configuration comes from `appsettings.json` plus an environment-specific overlay (`appsettings.Development.json` / `appsettings.Production.json`), selected by `ASPNETCORE_ENVIRONMENT` (set to `Development` in `Properties/launchSettings.json`; unset → Production for the deployed service).
- User secrets are enabled (`UserSecretsId` in the csproj) — use `dotnet user-secrets` for local credentials rather than committing them.

## Conventions

- `Nullable` and `ImplicitUsings` are enabled — common namespaces (e.g. `Microsoft.Extensions.Logging`, `System.Threading`) are available without explicit `using` statements.
- The project uses C# primary constructors (see `Worker(ILogger<Worker> logger)`); follow that style for new injected services.
- **Enums live in `BackupService/Enumerations/`** (namespace `BackupService.Enumerations`), one enum per file named after the enum (e.g. `ProfileStatus.cs`). Do not declare enums alongside the entity/component that uses them. The namespace is brought in via `@using BackupService.Enumerations` in `Components/_Imports.razor` for the component tree, and an explicit `using BackupService.Enumerations;` in non-Razor files that reference an enum.
- **Extension methods live in `BackupService/Extensions/`** (namespace `BackupService.Extensions`), e.g. `EnumExtensions.GetDescription(this Enum)` which returns a value's `[Description(...)]` text (falling back to the name). Give enum values shown in the UI a `[Description]` and render `value.GetDescription()` rather than the raw name. There is also a project-local `[HelpText("…")]` attribute (`Extensions/HelpTextAttribute.cs`) read by `EnumExtensions.GetHelpText(this Enum)` (returns the text, or empty string if absent) — used for the longer explanatory blurb shown under a dropdown (e.g. `OverwriteBehaviour`). The namespace is imported in `Components/_Imports.razor`. (Note: in the test project NUnit's global `using NUnit.Framework` makes `Description` ambiguous with `System.ComponentModel.DescriptionAttribute` — fully-qualify it there.)
- **Blazor components use code-behind.** A `.razor` file should hold only markup and the minimal C# unavoidable in markup (e.g. simple `@bind`/event wiring); put the bulk of the logic — fields, properties, lifecycle methods, event handlers, `[Inject]` services — in a sibling `.razor.cs` partial class (`public partial class Foo { ... }` in the component's namespace). Create the code-behind whenever a component needs more than trivial inline code.
- **Each page gets its own folder under `Components/Pages/`.** A routable page lives in `Components/Pages/<PageName>/<PageName>.razor`, with its code-behind and any page-specific files (sub-components, view models, partials) in that same folder. Examples: `Components/Pages/Dashboard/`, `Components/Pages/Settings/`, `Components/Pages/Authentication/` (the Login page). The folder becomes part of the component namespace (e.g. `BackupService.Components.Pages.Authentication`); match the code-behind's `namespace` to it. Routes come from `@page`, not the folder, so moving a page between folders doesn't change its URL. (Note: the page folder `Components/Pages/Authentication/` is distinct from the root `Authentication/` folder, which holds the auth *services*.)
- **For logging, use `Microsoft.Extensions.Logging`** — inject `ILogger<T>` via the constructor (already configured by the host; no third-party logging framework like Serilog or NLog). Use structured-logging message templates (e.g. `logger.LogInformation("Backed up {File}", path)`).
- **Use dependency injection throughout, via the built-in `Microsoft.Extensions.DependencyInjection` container** (available as `builder.Services` — no third-party container like Autofac). Register services on `builder.Services` (`AddSingleton`/`AddScoped`/`AddTransient`) and inject them via constructors rather than newing them up or using static/singleton access. Backup logic should live in injectable services (interface + implementation) consumed by `Worker`, not inline in `Worker` itself.
